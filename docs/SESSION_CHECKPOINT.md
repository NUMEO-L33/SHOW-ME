# ShowMe 재개 지점 — 2026-09-23

사용자가 Docker 오류 확인 후 `저장해`라고 요청하여 진행 상황을 저장했고, 이후 `docker 실행 완료`를 알려 실제 DB 검사를 재개했다. 이 문서는 최신 인계 기록이다. 원래 개발 계획의 목표나 완료 조건은 바꾸지 않는다. 아래 결과는 저장 시점의 관측이며 재개 시 최신 상태를 확인한다.

## 최신 검증 — 2026-09-23 실제 Replit DB + Storage 결합 검사 통과

사용자의 다음 단계 진행 승인에 따라 검증본 `b4efa09`를 기존 `codex/replit-migration-hardening` 브랜치에 커밋·푸시했다. 변경 없는 Replit 개발 작업본 `75774cf`를 같은 커밋으로 fast-forward했고, 해당 SHOW-ME 프로젝트에서 합성 통합 검사를 한 번 실행했다. 기존 post-merge hook이 없음을 확인했으며 의존성 설치·서버 재시작은 하지 않았다.

기존 runtime 계정·migration 이력·정리 권한 검증 후 새 UUID 합성 가이드 한 개와 진단 prefix의 네 객체만 사용했다. 실제 PostgreSQL + Replit Storage에서 원본 저장/재읽기, 실제 가림 PNG/썸네일, 원본 바이트/상태 보존, 게시 후 공유 철회와 JSON/이미지 404를 확인했다. **`PUBLICATION_INTEGRATION_CHECK PASS`, `PUBLICATION_INTEGRATION_EXIT 0`**이다. `applicationDatabaseUsed:true`, `database:"postgres"`, `databaseFixtureRemoved:true`, `remoteObjectsRemoved:true`, `localFixtureRemoved:true`, `pendingIO:false`를 모두 확보했다. 시험 가이드/연관 DB 행, 객체 네 개, 자체 임시 자료는 정리됐다.

검사 후 기존 API health **200**, 정확한 작업 폴더/entrypoint의 API 프로세스 **1개**, AI `off`, migration `verify-only`, Replit 저장소, AI 키/별도 operator·migration DB URL 없음과 깨끗한 git 상태를 확인했다. 새 소스의 진단 프로세스를 실행한 것이며 기존 앱 번들 재빌드/재시작까지 했다는 뜻은 아니다. 공개 실행기는 계속 OFF이고 진단 HTTP는 loopback만 사용했다. 개인 영상·외부 AI·운영 Publish, .env·키·권한·migration 변경은 없었다.

일반 1,056개/타입·빌드와 실제 로컬 PostgreSQL 137개는 앞선 단계의 결과다. 이번에는 이를 재실행했다고 표기하지 않는다. 원래 개발 MD의 목표·완료 조건을 바꾸지 않고 진행 기록만 갱신했다. **다음은 저장소 IAM/ACL 및 미확정 원격 쓰기의 운영 해소 조건 확인 → 공개 기능 활성화 범위 승인 → 실제 수신자/모바일 검증**이다. 이 통합 검사만으로 전체 E3/F1·공개 서비스·출시 완료로 표시하지 않는다.

## 이전 검증 — 2026-09-23 실제 PostgreSQL 137개 통과, Replit 결합 검사 준비

사용자의 점검 후 다음 단계 요청에 따라 `check-publication-integration.mjs`를 추가했다. 기존 개발 runtime binding/역할/이력과 정리 권한을 읽기 전용으로 확인하고, 새 UUID 합성 가이드 한 개와 진단 prefix의 네 객체로 기존 게시·철회·PNG 검사 경로를 실제 PostgreSQL에 연결한다. 세부 범위와 실행 인수는 `PUBLICATION_INTEGRATION_CHECK.md`.

기존 가이드를 스캔하거나 정리하지 않도록 진단 repository 메서드를 제한하고, 게시 복구/만료 검색에 SQL WHERE 단계의 guideId 범위를 추가했다. 기본 제품 검색 동작과 공개 OFF 상태는 유지한다. 실제 DB seed는 한 트랜잭션으로 ready까지 확정하도록 했고, 정리 때 새 가이드 토큰 소유권·원격 객체 부재·자기 ID의 연관 DB 행 부재를 확인한다. 미완료 I/O는 완료로 처리하지 않고 manifest를 유지한다. 신규 migration이나 역할/권한 변경은 없다.

일반 검사 **1,056개**(이관 101 / 서버 839 / 화면 116), 실패·취소·생략 0, 최종 서버 전체 재검증 종료 코드 0. 새 집중 6개, API 제품/테스트 타입 검사, 서버 빌드, diff 공백 검사도 통과했다. 첫 새 검사 두 개의 스냅샷 시점 오류는 시험 직전 상태를 기준으로 수정했다. 첫 빌드의 Windows 샌드박스 경로 접근 거부는 같은 명령의 권한 있는 실행으로 해소했고 설치/잠금 파일은 바꾸지 않았다.

최초 준비 때는 Docker CLI 접근 거부 후 권한 있는 조회에서도 Linux 엔진 named pipe가 없어 실제 DB 실행을 보류했다. 사용자 `ㄱ`으로 재개한 뒤 Docker 29.7.2 연결을 확인하고, 기존 로컬 PostgreSQL 이미지로 일회용 DB runner를 실행했다. **실제 PostgreSQL 16.15 검사 137개 전부 통과**, 실패·취소·생략 0, 종료 코드 0이며 migration 0000–0017을 확인했다. 새 성공/렌더 실패 검사 두 개에서 다른 가이드와 queued 작업 보존, 시험 자료 정리를 통과했다. 이전 135개 결과나 JSON 대체 검사로 대신한 것이 아니다.

이번 실행 컨테이너 `showme-b5-5bbbb3ef35c443b48a0d15e20db29be1`의 `postgres_fixture_removed`, `disposableDataRemoved:true`를 확인했다. 기존 이미지·다른 컨테이너·사용자 파일은 삭제하지 않았으며 다운로드/설치·영구 호스트 마운트도 없었다. 이번 재개는 DB 검사와 기록 갱신만 했고, 일반 1,056개/타입/빌드는 앞선 검증 결과를 유지한다.

변경은 로컬 미커밋·미푸시다. 원래 개발 MD 목표·완료 조건, .env/키, 기존 Replit 앱/DB/Storage, 외부 AI, 개인 영상, 운영 Publish는 변경하거나 사용하지 않았다. **다음은 검증본의 커밋·푸시 및 Replit 개발 작업본 반영 → 승인된 합성 DB+Storage 결합 실행**이다. 이번 실제 DB 검사는 로컬 PostgreSQL + 임시 LocalStorage이며 Replit 결합 검사나 공개 활성화/출시 완료가 아니다.

## 이전 검증 — 2026-09-22 격리된 합성 Replit 저장소 게시·철회 통과

사용자의 다음 단계 진행 승인에 따라 검사 도구 `a2ba9b8`을 기존 브랜치에 커밋·푸시하고 변경 없는 Replit `ebcace4`에서 fast-forward했다. 제품 코드·기존 API 프로세스·.env·키·권한·원래 개발 MD는 변경하지 않았다. 검사 도구 설명은 `PUBLICATION_STORAGE_CHECK.md`.

합성 JPEG만 새 진단 prefix에 저장하고 재읽기/materialize 일치, 실제 FFmpeg 가림 PNG/썸네일의 예상 바이트, 원본 보존, 게시 후 공유 중지의 JSON/이미지 404를 확인했다. 처리본 정리와 마지막 네 객체의 `exists:false`/진단 prefix 비어 있음까지 확인하여 **`PUBLICATION_STORAGE_CHECK PASS`, `PUBLICATION_STORAGE_EXIT 0`**이다. `remoteObjectsRemoved:true`, `localFixtureRemoved:true`, `pendingIO:false`; 테스트 객체 네 개와 자체 임시 자료는 모두 정리됐다.

metadata는 별도 임시 JSON repository, HTTP는 loopback 임의 포트만 사용했다. **실제 Replit 앱 DB와 저장소를 함께 쓰는 통합 게시 검사나 인터넷 공개 활성화가 아니다.** 개인 영상·기존 가이드·외부 AI·운영 Publish는 사용하지 않았다. 기존 앱의 공개 실행기는 계속 OFF다.

검사 후 기존 health **200**, 정확한 API 작업 폴더/entrypoint의 실행기 **1개**, AI off, migration verify-only, Replit 저장소, AI 키/별도 관리자 DB 환경 없음과 변경 없는 git 상태를 확인했다. 최초 프로세스 탐지의 상대 경로 비교와 PID 전사 오류는 성공으로 취급하지 않고 실제 entrypoint 기준으로 다시 검증했다. 서버 재시작은 하지 않았다.

로컬 최종 집중 검사 **67개**, 실패·취소·생략 0, API 제품/테스트 타입 검사 통과. 새 검사 도구의 canonical key/함수명/초안 검토 시각 기준 오류는 로컬에서 수정한 뒤 Replit에 반영했다. 이번 턴에 전체 1,044개나 PostgreSQL 135개를 다시 실행했다고 표기하지 않는다.

**다음은 실제 DB+저장소 결합 검사와 공개 활성화 조건 확정이다.** 저장소 전체 IAM/ACL, 미확정 원격 쓰기 운영 해소, 실제 수신자/모바일 확인도 남는다. 원래 E3/F1 목표·완료 조건은 유지하며 전체 완료/출시 완료로 표시하지 않는다.

## 이전 반영 — 2026-09-22 Replit 개발 코드·DB·권한·재시작 완료

사용자가 직전 제안한 커밋·푸시 → Replit 개발환경 업데이트 → migration/기존 역할 최소 권한 → API 재시작 범위를 승인했다. 기능 작업 `feb29ec`와 검사 보완 `227a7ef`를 기존 `codex/replit-migration-hardening` 브랜치로 푸시·반영했다. 원래 개발 MD의 목표·완료 조건은 변경하지 않았다.

첫 Replit 전체 검사의 CLI 시작 timeout, 비추적 실제 JPEG 읽기 오류, 비동기 read 개수 경합 3건은 기록하고 DB 반영을 보류했다. CLI 지연 import, 테스트 실제 시작 신호/모의 시계, 파일 단위 직렬 실행을 보완한 후 로컬과 Replit 일반 전체 **1,044개**(이관 100 / 서버 828 / 클라이언트 116), 실패·취소·생략 없이 통과했다. 로컬 API 타입/빌드, Replit workspace 타입 검사와 전체 빌드도 통과했다. PostgreSQL 135개는 앞선 별도 일회용 DB 검증 결과이며 이번에 다시 실행한 것으로 표기하지 않는다.

Replit API/web를 중지하고 정확한 프로젝트·DB 대상·기존 13개 이력을 검증한 뒤 migration **0013–0017 적용 및 18개 이력 일치**를 확인했다. 기존 guides/guide_steps/guide_drafts 행 수는 전후 각각 **1/1/1**이다. 새 5개 테이블의 필요한 권한만 기존 runtime 계정에 부여했고 publication UPDATE와 과도한 권한 거부 및 기존 역할 전체 검사를 실제 연결로 확인했다. 새 계정·키·비밀번호·.env는 변경하지 않았다.

API/web 재시작 후 API 프로세스 **1개**, runtime binding 일치, migration `verify-only`, AI `off`, AI 키/별도 관리자 DB 환경 없음, health **200**, Preview 새로고침 초기 화면을 확인했다. 공개 실행기와 외부 AI는 계속 **OFF**이며 Replit Publish는 누르지 않았다. 초기 UI 재시작 확인 중 API 0개가 관측된 상태는 완료로 취급하지 않고 재실행 후 검증했다. 상세와 남은 제한은 `REPLIT_PUBLICATION_READINESS.md`.

**다음은 합성 자료만 쓰는 Replit 저장소 쓰기·재읽기·정리 및 게시/철회 실환경 검증 범위 확정과 활성화 검토다.** 아직 공개 서비스나 E3/F1 전체 완료가 아니다. IAM/ACL 확인, 불확실 원격 쓰기의 운영 해소, 실제 수신자/모바일 검증도 남는다. 이번 반영에서는 신규 합성 업로드·개인 영상·실제 외부 AI 전송을 하지 않았다.

## 이전 점검 — 2026-09-22 Replit 반영 전 읽기 전용 확인

기존 SHOW-ME 개발환경을 조회했다. Replit은 변경 파일 없는 `a2aecd9`이며 현재 DB migration 13개(0000–0012)가 해당 소스와 정확히 일치한다. 로컬 새 코드에 필요한 0013–0017과 새 테이블 5개는 미반영이다. 기존 앱 DB 계정 연결·전체 최소 권한 검사, 실행 API 1개의 runtime binding/verify-only/AI off 확인, health 200, 명시적 Replit bucket의 인증 목록 조회를 통과했다. 저장소는 임의의 빈 진단 prefix만 조회했고 개인 객체 내용은 읽지 않았다. 전체 IAM 감사나 처리본 쓰기/삭제 검증은 아니다. 상세는 `REPLIT_PUBLICATION_READINESS.md`.

이번에는 Replit 파일·DB·역할·키·설정을 변경하지 않았고 pull/배포/재시작/공개 활성화/AI 호출도 하지 않았다. 로컬은 기존 미커밋 작업을 유지하고 점검 기록만 추가했다. **다음은 실제 변경 승인을 받은 뒤 검증본 커밋·푸시, Replit 개발 코드/DB 5개 변경/기존 역할 최소 권한 반영 및 API 재시작이다.** 공개 기능 활성화와 합성 실환경 검증은 별도로 구분한다. 기존 개발 MD의 목표·완료 조건은 그대로다.

## 최신 구현 — 2026-09-22 게시·공유 관리와 공개 뷰어

실제 편집기의 게시·상태·공유 중지와 `/g/:slug` 공개 뷰어를 기존 서버 API에 연결했다. 저장 완료와 정확한 개인정보 확인·명시적 동의가 있어야 새 게시를 요청한다. 요청 ID는 전송 전에 브라우저에 기록하고 불확실 응답/재접속은 같은 ID로 조회만 한다. 충돌 시 자동 재게시/최신 버전으로의 숨은 중지를 하지 않는다. 원본 공유는 false로 유지한다. 상세는 `PUBLICATION_UI.md`.

공개 화면은 확정된 공개 DTO/처리 PNG만 사용하며 편집 데이터·브라우저 예시로 대체하지 않는다. 같은 출처의 공개 GET을 private credential transport와 분리했다. 이미지 하나씩 조회, 이전/다음·확대·키보드 이동, 재조회/만료/철회 오류 화면을 추가했다. 편집용 원본 미리보기와 실제 공유 링크를 구분하고, 예시 버튼은 가짜 공유 완료 대신 인앱 예시로 이동한다.

일반 전체 **1,043개 통과**(이관 99 / 서버 828 / 클라이언트 116), 실패·취소·생략 0, 종료 코드 0. 화면 제품/테스트 타입 검사 및 production 빌드 통과. 기존 Windows 빌드 누락은 임시 폴더의 정확한 native 패키지 3개와 명령별 NODE_PATH로 해소했으며 pnpm 설정/잠금 파일은 변경하지 않았다. 약 601KB 청크 및 sourcemap 경고는 남는다. 실제 PostgreSQL 135개는 직전 단계 결과이며 이번 화면 작업에서 재실행하지 않았다.

전용 loopback 합성 fixture로 실제 게시 API·실제 PNG 준비·확정 링크·확대/단계 이동·초안 수정 후 재게시 차단/이전 공개본 유지·공유 중지 후 기존 링크 차단을 브라우저에서 확인했다. PC 1280×800/모바일 폭 393×852와 좁은 창을 확인했지만 실제 휴대폰/카카오톡/외부 운영 링크 검증은 아니다. 첫 fixture CORS 임의 포트 누락과 새 테스트의 readonly 타입 오류를 수정했다. 첫/두 번째 합성 서버 및 그 시험 자료는 모두 정리했다.

**기본 서버의 공개 실행기는 계속 OFF다.** 다음은 Replit migration·기존 역할·Storage와 중지/복구 조건 확인, 제한된 합성 실환경 검증 및 활성화 검토다. 미확인 게시/원격 쓰기의 운영 해소, 공유 미리보기·원본 별도 opt-in, 실제 수신자/모바일 검증도 남는다. E3/F1 전체나 출시 완료로 표시하지 않는다. 원래 개발 MD 목표·완료 조건, .env·키·개인 영상·실제 AI·Replit DB·배포는 변경하지 않았고 로컬 미커밋·미푸시다.

## 이전 구현 — 2026-09-21 게시 실행기와 선택적 서버 lifecycle

`DurablePublicationRuntime`이 저장된 queued 요청을 찾아 실제 가림 PNG 준비 → 이번 호출의 lease로 원자적 게시 확정까지 수행한다. 기존 복구/철회/15일 만료 정리를 같은 예약 루프에 묶었다. 독립 cursor가 실패한 앞 작업 뒤로 진행하며, 과거 running/assets-ready lease를 재사용하거나 이미 시작된 원격 업로드를 반복하지 않는다. commit 응답 유실은 조회로만 확인한다. 상세는 `PUBLICATION_RUNTIME.md`.

서버의 선택적 `createPublication` factory를 API·시작 검증·종료에 연결했다. API와 실행기는 같은 DB/Storage 인스턴스를 사용하며, 시작 전/종료 후 접수를 차단한다. AI와 게시의 접수 게이트는 둘 다 즉시 닫는다. 정상 진행 중인 정리는 새 접수를 막지 않되 timeout 뒤 미완료 I/O는 슬롯과 증거를 유지한다. 종료에는 제한된 정리 대기를 두고 실제 작업이 남으면 `PUBLICATION_STOP_PENDING`으로 보고한다.

**기본 bootstrap은 실행기를 주입하지 않는다.** 기본 새 게시 접수 503과 화면의 공개 준비 상태를 유지했다. 선택적 제품 lifecycle 코드/합성 검사와 운영 공개 활성화는 다르다. 서버 중지는 공유 철회가 아니며, 이미 발행된 DB commit의 롤백까지 보장하지 않는다. 공유 중지가 먼저 확정되면 늦은 게시를 차단하는 것은 별도 저장 계층의 비교/잠금 계약이다.

최종 일반 전체 **1,031개 통과**(이관 99 / 서버 828 / 클라이언트 104), 실패·취소·생략 0, 종료 코드 0이다. 실행기 집중 **18개**, API와 합친 집중 **34개**도 통과했다. 서버 제품/테스트 타입 검사·빌드·diff 공백 검사 통과. 마지막 점검에서는 저장 오류와 DB monitor가 겹칠 때 실제 monitor가 끝나기 전에 writer 슬롯을 반환하지 않도록 보완했다. **이 보완과 후속 API 테스트 수정까지 포함한 최종 코드로 실제 PostgreSQL 16.15 검사 135개도 다시 통과했다**(migration 0000–0017, 실패·취소·생략 0, 종료 코드 0). 일반 전체는 직전 실행 결과이며 이번 재개에서는 DB 전체를 재검증했다.

후속 재검증에서 기존 `owner API bounds non-cooperative admission...` 검사 실패를 관측했다. 이어진 사용자 메시지 후 실행 세션과 상세 출력에 접근할 수 없어 그 실패 원인을 확정하지 않는다. 코드 점검에서 실제 저장 완료 전에 30ms 경과만 가정하던 잠재 경합을 확인했고, 제품 제한시간을 바꾸지 않고 저장 완료 신호 뒤 모의 시각을 진행하도록 테스트를 고쳤다. 이후 집중/일반 전체를 위 수치로 다시 통과했다.

앞선 DB 재실행은 완료/정리 출력을 확보하지 못했고, 당시 컨테이너는 `showme-b5-13ec368148334d44aa717791574ef68d`였다. Docker 엔진 named pipe가 사라져 상태를 확인할 수 없었다. 이번 재개에서 `docker desktop start --timeout 45`로 Docker Desktop을 시작한 뒤 엔진 29.7.2 연결과 **해당 정확한 컨테이너가 없음을 확인**했다. 이전 실행의 미확보 결과를 통과로 추정하지 않고 새 컨테이너 `showme-b5-5a6976c074b5431e8aa0b9dd6d9b0d27`로 전체 135개를 재검증했다. `postgres_fixture_removed`와 `disposableDataRemoved:true`로 이번 일회용 DB 정리를 확인했다. Docker 초기화·기존 데이터 삭제는 하지 않았다.

처음 새 집중 검사 3건 실패에서 정상 정리와 timeout 뒤 미완료 I/O를 구분하지 못한 접수 게이트, 종료 직후 임시 폴더 정리 경합을 고쳤고 잘못된 재게시 fixture 필드도 수정했다. 미응답 저장/DB 확인에서는 종료 완료를 추정하지 않도록 별도 회귀 검사를 추가했다. 첫 임시 DB 컨테이너 `showme-b5-0d64d4923c484a8e8e55abb17fde48e6`은 검사 후 정리했다.

다음은 **게시·진행 상태·공유 중지/공개 뷰어 화면 연결과 합성 브라우저 검사 → Replit migration·기존 역할·Storage 실환경 검증 및 기본 활성화**다. 불확실 원격 쓰기의 운영 해소도 남는다. 기존 Windows 화면 빌드 의존성 문제는 이번 서버 작업에서 변경하지 않았고 UI/브라우저 검증도 수행하지 않았다. E3 전체나 운영 배포 완료가 아니다.

추가 migration 없이 앞선 0017/JSON v9를 유지한다. 기존 개발 MD의 목표·완료 조건은 변경하지 않았으며 진행 기록만 보완했다. 앞선 작업과 함께 로컬 **미커밋·미푸시**다. .env·키·개인 영상·실제 외부 AI, Replit DB/역할/pull/재시작/배포는 건드리지 않았다. 합성 자료와 로컬 일회용 DB만 사용했다.

## 이전 구현 — 2026-09-21 게시·공유 중지 API와 공개 처리본 응답

기존 E3 내부 저장/처리/철회를 소유자 인증 HTTP와 공개 처리본 HTTP로 연결했다. 게시 접수·같은 요청 ID의 결과 복구·관측한 상태에 대한 공유 중지, 현재 활성/미만료 게시본만 반환하는 공개 JSON과 이미지 경로를 추가했다. 공개 응답은 허용 목록이며 내부 단계 ID도 공개용 순번으로 분리한다. 이미지 전체 receipt 검증과 읽은 뒤 공개 권한 재확인을 통과하기 전에는 이미지 바이트를 보내지 않는다. 상세는 `PUBLICATION_API.md`.

화면용 상태 조회/게시/공유 중지 요청 함수를 추가했다. 같은 출처·응답 구조·고정 만료·요청 ID를 검사하며 자동 재게시/최신 상태로의 숨은 재시도는 없다. **실제 편집기 버튼·공개 뷰어·게시 실행기/복구 자동 시작은 미연결이다.** API 경로는 등록했지만 기본 서버는 새 게시 요청을 503으로 거부한다. 기존 저장된 요청 조회/철회와 내부에서 이미 확정한 활성 게시본 접근만 가능하다. `canRequest`는 개인정보 확인 완료를 대신하지 않는다.

최종 일반 전체 **1,013개 통과**(이관 99 / 서버 810 / 클라이언트 104), 실제 PostgreSQL 16.15 **133개 통과**(migration 0000–0017), 모두 실패·취소·생략 0, 종료 코드 0. 새 API 집중 16개와 클라이언트 5개를 추가했다. 서버·화면의 제품/테스트 타입 검사, 서버 빌드, diff 공백 검사도 통과했다. 최종 일회용 컨테이너 `showme-b5-ac6a48e5dbaa4203ae5c02c78841de79`와 시험 DB는 `postgres_fixture_removed`로 정리를 확인했다.

첫 DB 전체 실행은 132/133 통과였다. 내부 단계 ID에 포함된 가이드 ID가 공개 응답으로 나간 검사가 실패하여, 공개용 `step-1` 순번과 내부 ID를 분리했다. 일반 fixture에도 같은 조건과 내부 ID 접근 거절을 추가한 뒤 일반/실제 DB 전체를 다시 통과했다. 첫 컨테이너 `showme-b5-9519d626f0a84de0b44f016e0026bb27`도 정리했다. 새 타입 인자 누락과 JSON 속성 순서에 의존한 테스트 비교도 수정했다.

**화면 빌드는 이번 PC에서 완료하지 못했다.** 최초 명령의 Vite 경로를 패키지 내부 경로로 바로잡은 뒤, 설치된 Rollup 4.63.1의 `@rollup/rollup-win32-x64-msvc` 누락으로 시작이 중단됐다. 기존 pnpm lock/workspace 설정이 Windows 네이티브 선택 의존성을 제외한다. 잠금 파일/설정/설치를 임의 변경하지 않았으며 이번 화면 빌드 통과로 기록하지 않는다. 화면 코드 변경은 아직 미사용 요청 함수이고 클라이언트 104개·타입 검사는 통과했다. 브라우저 UI 검증은 수행하지 않았다.

다음은 **게시 실행기/복구의 제품 lifecycle → 게시·공유 중지/공개 뷰어 UI 및 합성 브라우저 검사 → Replit migration·기존 역할·Storage 실환경 검증**이다. 불확실 원격 쓰기 해소와 운영 부하 검증도 남아 있다. 철회는 새 접근을 차단하는 것이며 이미 내려받은 파일을 회수한다는 뜻은 아니다. E3 전체/출시 완료나 Replit 적용 완료가 아니다.

이번 단계에 추가 migration은 없고 앞선 0017/JSON v9를 유지한다. 원래 개발 MD 목표·완료 조건은 바꾸지 않았으며 진행/계약 기록만 보완했다. 앞선 변경과 함께 로컬 **미커밋·미푸시**다. Replit DB/역할/pull/배포/재시작, .env·키 수정, 개인 영상·실제 외부 AI 호출을 하지 않았다. 합성 자료와 로컬 일회용 DB만 사용했다.

## 이전 구현 — 2026-09-21 비공개 7일 / 게시본 15일 보존 분리

기존 비공개 정리 루프를 게시 상태와 분리했다. 마지막 정상 편집 저장 후 7일 정리 때, 게시 이력이 있는 가이드는 원본 정리 목록을 먼저 저장하고 초안·단계·AI 실행/결과·원본 메타데이터를 제거한다. 게시되지 않은 작업은 취소하고 설명 내용을 제거하며, 활성 게시본은 첫 게시+15일 동안 유지한다. 게시·조회·정리 재시도가 원본 보관 시각을 늘리지 않는다. 상세는 `PRIVATE_RETENTION.md`.

비공개 만료 뒤에는 새 편집·분석·미디어 재처리·게시 준비 및 과거 원본 이미지 티켓 접근을 거부한다. 공유 철회·전체 삭제는 계속 가능하다. 원본 삭제 실패나 미확정 처리본 writer가 있으면 정리 목록을 남기고 재접속 후 재시도한다. 유효 게시가 없어지면 기존 전체 삭제로 연결한다. 저장 경합은 부모 잠금과 관측 시각 비교로 막고, DB의 모든 만료 쓰기는 반영 건수를 검사한다.

최종 일반 전체 **992개 통과**(이관 99 / 서버 794 / 클라이언트 99), 실제 PostgreSQL 16.15 **132개 통과**(migration 0000–0017), 실패·취소·생략 0, 종료 코드 0. 서버 제품/테스트 타입 검사·빌드·diff 공백 검사 통과. 추가 일반 12개 및 실제 DB 5개가 이번 보존 분리를 검증한다. 일회용 컨테이너 `showme-b5-deb852eb524b4a1ca99720a50487e9f3`은 `postgres_fixture_removed`로 정리를 확인했다.

검사 중 새 fixture의 개인정보 확인 해제/settle 인자/타입 문제를 수정했다. 일반 전체에서 기존 DB 모형이 새 정리 테이블 조회를 처리하지 못한 문제도 모형에 반영한 뒤 전체를 다시 통과했다. 첫 직접 집중 실행은 `NODE_ENV=test` 누락으로 시작 가드가 실행되어 실패했으며, 테스트 환경을 명시해 재검사했다. Docker가 꺼져 첫 DB 시작은 실패했고, 사용자의 실행 완료 확인 후 임시 DB 검사를 마쳤다. 운영 환경의 정상 동작 증거로 혼동하지 않는다.

새 migration 0017과 JSON v9를 추가했다. 구버전 JSON은 읽을 때 파일을 바꾸지 않고 다음 저장에서 변환한다. 새 역할 생성 코드에 정리 테이블 권한을 포함하지만 **기존 Replit DB/역할에는 적용하지 않았다.** 기존 SQL/원래 개발 MD 목표·완료 조건은 변경하지 않았다. 앞선 구현과 함께 로컬 **미커밋·미푸시**다. .env·키·개인 영상·외부 AI·배포·재시작은 건드리지 않았다.

다음은 **인증된 게시/철회/상태 API와 공개 이미지 권한 → 실행 루프·화면 연결 → 합성 Replit 실환경 검증**이다. 7일은 기존 주기적 정리 정책이고 서버 중지/저장소 장애/미확정 쓰기에서 물리 삭제가 늦어질 수 있다. 불확실 원격 쓰기 해소 절차도 남아 있다. 게시 HTTP·공개 DTO·자동 게시·전용 UI는 미연결이며 E3 전체나 운영 완료가 아니다.

## 이전 구현 — 2026-09-21 공유 철회와 게시 만료

내부 `stopPublication`·`getAccessiblePublication`·만료 순환 조회를 추가했다. 철회는 관측한 head version/job ID를 비교하여 공개 참조 제거·진행 중 게시 취소·처리본 cleanup 전환을 한 번에 저장한다. 뒤늦은 worker/과거 commit/오래된 이미지 ID가 게시를 되살리지 못한다. 정확한 첫 게시+15일 경계부터 새 접근과 준비를 거절하며, 내부 복구 루프가 만료 참조를 철회한 뒤 처리본을 정리한다. 삭제 실패/불확실 writer는 원장을 유지한다. 상세는 `PUBLICATION_LIFECYCLE.md`.

최종 일반 전체 **980개 통과**(이관 99 / 서버 782 / 클라이언트 99), 실제 PostgreSQL 16.15 **127개 통과**(migration 0000–0016), 모두 실패·취소·생략 0, 종료 코드 0. 집중 검사 40개, 최종 서버 제품/테스트 타입 검사·빌드·diff 공백 검사도 통과했다. 새 mock의 자료형 오류를 고쳤고, 마지막 PostgreSQL 시각 조회 위치 보완 후 일반/DB 전체를 재실행해 같은 결과를 확인했다. 최종 일회용 컨테이너 `showme-b5-3de423fce2334908ae5d013b92105d11`과 시험 DB는 `postgres_fixture_removed`로 정리를 확인했다.

**7일 비공개 자료와 15일 게시본의 보존 분리는 아직 미완료다.** 기존 제품 7일 정리가 부모 전체를 지우므로, 별도 비공개 만료/정리 상태와 분석·편집 차단을 연결하기 전에는 게시를 활성화하면 안 된다. 이번 만료 조회는 내부 권한 판단이며 HTTP/공개 DTO/이미지 스트리밍 권한·no-store까지 연결된 것은 아니다. 다음은 보존 분리 → 인증 API/실행 루프/화면 → 합성 실환경 검증이다. E3 전체나 운영 완료가 아니다.

새 migration 0016은 만료 cursor 인덱스만 추가하고 JSON v8을 유지한다. 제품 시작/공개/버튼은 계속 미등록이다. 기존 SQL migration/원래 개발 MD 목표·완료 조건은 바꾸지 않았다. 앞선 구현과 함께 로컬 **미커밋·미푸시** 상태다. Replit DB/역할/pull/재시작/배포, .env·키·개인 영상·외부 AI는 사용하지 않았다. 합성 자료와 로컬 임시 DB만 검사했다.

## 이전 구현 — 2026-09-21 게시 확정의 원자적 저장

E3 내부 `commitPublication`을 추가했다. 모든 가림 처리본의 준비/소유권/현재 초안·확인을 재검사한 뒤 불변 게시본, 활성 참조, 작업 성공, 이전 자산 정리 전환을 한 번에 저장한다. 실패 시 이전 게시본을 유지하고, 중복 요청은 재게시하지 않으며, 활성 처리본은 일반 정리에서 보호한다. 상세는 `PUBLICATION_COMMIT.md`.

최종 일반 전체 **966개 통과**(이관 99 / 서버 768 / 클라이언트 99), 실제 PostgreSQL 16.15 **122개 통과**, 모두 실패·취소·생략 0, 종료 코드 0. 집중 검사 38개, 서버 제품/테스트 타입 검사·빌드·diff 공백 검사 통과. 일회용 컨테이너 `showme-b5-1a7cb9580f2244699ba78d986193309d`과 시험 DB는 정리했다. 합성 이미지·LocalStorage 시험이며 운영 Replit 검증은 아니다.

**아직 제품 공개 기능은 켜지 않았다.** 첫 게시+15일의 고정 시각은 저장하지만 공개 접근/철회/만료 정리와 제품 lifecycle은 다음이다. 기존 7일 미공개 정리는 부모 전체를 삭제하므로, 이를 원본/초안 정리와 게시 처리본 보존으로 분리하기 전에는 게시를 활성화하면 안 된다. 원본 보존을 무단 연장하지 않았다. 내부 `getPublicationState`는 인증·만료 필터 없는 저장 조회이며 공개 DTO로 쓰면 안 된다.

다음은 공유 철회·7일/15일 보존 분리·만료 → 인증 API/작업 실행/화면 연결 → 합성 실환경 검증이다. 원격 put 종료가 불확실한 기록의 운영 해소도 남아 있다. E3 전체나 운영 완료가 아니다.

새 migration 0015·JSON v8을 추가했다. 기존 SQL migration/원래 개발 MD 목표·완료 조건은 수정하지 않았다. 앞선 변경과 함께 로컬 **미커밋·미푸시** 상태다. Replit DB/역할/pull/재시작/배포, .env·키·개인 영상·외부 AI는 건드리지 않았다.

## 이전 구현 — 2026-09-21 게시 준비 작업의 재시작 정리

`PublicationRecoveryWorker`와 종류별 최대 20개 batch UUID 순환 조회를 구현했다. 재시작 후 만료된 running job을 실패 처리하고 terminal job의 남은 출력 자산을 정리한다. 미확인 writer/실패 선두가 다음 정리를 가리지 않으며, queued/유효 lease/초안/원본/다른 묶음은 보존한다. DB/저장소 작업이 제한시간 뒤에도 끝나지 않으면 추가 요청을 쌓지 않고 중단 상태에 `pendingIO`를 남긴다. PostgreSQL 자산 저장/삭제의 RETURNING도 확인하여 조용히 무시된 쓰기를 성공으로 처리하지 않는다. 상세는 `PUBLICATION_RECOVERY.md`.

최종 일반 전체 **952개 통과**(이관 99 / 서버 754 / 클라이언트 99), 실제 PostgreSQL 16.15 **118개 통과**, 모두 실패·취소·생략 0, 종료 코드 0. 서버 제품/테스트 타입 검사·빌드·diff 공백 검사도 통과했다. 처음 새 검사 4개 실패는 다중 가이드 fixture의 extracting 전환 누락이었고 fixture만 고친 후 전체 재실행이 통과했다. 일회용 컨테이너 `showme-b5-d6cbd283269e4345a4da0735d02cee01`과 합성 시험 DB는 정리했다.

**명시적으로 시작하는 내부 정리 루프이며 제품 시작/HTTP에는 미등록이다.** 원격 put 도중 프로세스가 죽어 실제 종료를 확인할 수 없는 writer는 종료/삭제 완료를 추정하지 않고 원장·삭제 차단을 보존한다. 이 불확실성의 Replit Storage 검증/운영 해소 절차는 남아 있다. 자동 재업로드·공개·새 claim은 실행하지 않는다.

다음 제품 코드는 불변 게시본·원자적 활성 참조 교체 → 공유 철회 → 첫 게시 후 15일 만료다. 그 후 준비 실행/정리 루프의 제품 lifecycle과 인증 API를 연결하고 실환경 검증을 해야 한다. E3 전체 완료나 운영 배포 완료가 아니다.

추가 migration 없이 0013/0014·JSON v7을 사용한다. 앞선 작업과 함께 로컬 **미커밋·미푸시** 상태다. Replit DB/역할/pull/재시작/배포, .env·키·개인 영상·외부 AI 및 기존 개발 MD 목표/완료 조건은 변경하지 않았다.

## 이전 구현 — 2026-09-21 게시 준비와 실제 가림 저장 연결

`preparePublicationAssets`가 job의 기존 예약 묶음/단일 writer를 E2 실제 PNG 생성·저장 엔진에 연결했다. 별도 묶음을 중복 예약하지 않으며 마지막 이미지 receipt와 job assets-ready를 같은 트랜잭션에 기록한다. 처리 중 편집/취소/삭제를 감지하고, 취소를 무시한 저장은 실제 종료 뒤 정리한다. 응답 유실 시 이미지 업로드를 반복하지 않는다. 상세는 `PUBLICATION_PREPARATION.md`.

일반 전체 **940개 통과**(이관 99 / 서버 742 / 클라이언트 99), 실제 PostgreSQL 16.15 **115개 통과**, 모두 실패·취소·생략 0, 종료 코드 0이다. 집중 검사 42개와 서버 제품/테스트 타입 검사·서버 빌드도 통과했다. 새 DB 검사는 실제 합성 이미지 변환+LocalStorage+DB 준비 상태를 연결했고 마지막 쓰기의 예외/무시에서 동시 롤백·정리, 다른 연결의 취소와 늦은 put을 검증했다. 일회용 컨테이너 `showme-b5-826b5e03897d440a8c54a4af84d5a1f9` 정리를 확인했다. 빌드의 샌드박스 상위 경로 접근 제한은 동일 빌드의 권한 환경 재검사로 구분하여 기록했다.

**공개 완료가 아니라 내부 이미지 준비 완료다.** 공개 참조·HTTP·버튼·자동 작업 루프는 아직 연결하지 않았다. 원격 쓰기 도중 프로세스 자체가 죽은 경우는 종료를 추정하지 않고 원장/삭제 차단을 유지한다. 다음은 자동 작업/재시작 정리와 불확실 원격 쓰기 해소, 그 뒤 불변 게시본 확정·철회·첫 게시 후 15일 만료다. E3 전체 완료가 아니다.

이 연결은 추가 migration 없이 앞선 0013/0014·JSON v7을 사용한다. 앞선 원장 변경과 함께 로컬 **미커밋·미푸시** 상태다. Replit migration/역할/pull/재시작/배포, .env·키·개인 영상·외부 AI는 건드리지 않았다. 기존 개발 MD 목표·완료 조건은 그대로이며 진행 기록만 보완했다.

## 이전 구현 — 2026-09-21 게시 작업 내구 원장

E3 첫 부분으로 게시 요청/불변 문구 snapshot/자산 정리 소유권의 동시 저장, 요청 재전송 보호, 독립된 단일 writer lease, 비공개 assets-ready 단계, 수정/취소/삭제 경합 차단, 만료 작업 조회·취소를 구현했다. 상세는 `PUBLICATION_JOB_LEDGER.md`. **실제 게시 성공/공개 링크/자동 worker/HTTP/버튼은 아직 연결하지 않았다.**

실제 PostgreSQL 16.15 **111개 통과**, 실패·취소·생략 0이며 일회용 DB 컨테이너 정리를 확인했다. 최종 일반 전체 **924개 통과**(이관 99 / 서버 726 / 클라이언트 99), 실패·취소·생략 0, 종료 코드 0이다. 관련 31개, 서버 제품/테스트 타입 검사와 빌드도 통과했다. 첫 일반 전체 실행의 기존 30ms DB probe 타이밍 가정을 테스트의 제어된 시각/시작 동기화로 고쳤고, 제품 제한시간은 바꾸지 않았다.

새 migration 0014, JSON v7을 추가했다. v6 자산 원장은 보존한다. 이번 변경은 로컬 작업본에 저장했으며 아직 커밋/푸시하지 않았다. Replit migration/실행 역할 권한 변경·pull·재시작·배포는 하지 않았다. .env·키·개인 영상·실제 AI도 건드리지 않았다. 기존 MD 목표/완료 조건은 그대로다.

다음은 job writer와 실제 E2 저장 엔진의 연결/복구, 그 뒤 불변 게시본·활성 참조·공유 철회·첫 게시 후 15일 만료다. 아직 불확실한 원격 쓰기는 자동 해소하지 못하며, lease가 만료돼도 자산 삭제 완료를 막는 기록을 유지한다. E3 전체 완료가 아니다.

## 이전 구현 — 2026-09-21 가림 처리본 내구 저장 엔진

E2 내부 저장 함수와 전체 삭제 연결을 구현했다. 저장 전에 모든 처리본의 정리 소유권을 `guide_assets`에 기록하고, 단일 writer가 실제 PNG/썸네일을 비공개 저장·재읽기 검증한 뒤 현재 revision/개인정보 확인을 다시 검사한다. 수정/삭제/실패는 cleanup으로 보내며 미정리 자산이 남으면 가이드 삭제를 완료하지 않는다. 자세한 계약·한계는 `PRIVACY_ASSET_STORAGE.md`.

일반 **911개**, 실제 PostgreSQL 16.15 **106개** 통과(실패·취소·생략 0). 이후 최종 임시 파일 경로 보완은 관련 14개 재검사·서버 제품/테스트 타입 검사·서버 빌드 통과로 확인했다. 일회용 DB/컨테이너는 정리했다. 이번에는 서버 기능이며 새 화면/브라우저 검증은 없다. 초기 모듈 순환, 구버전 fixture, migration 개수 기대값 문제를 수정해 재검사했다.

**아직 활성화하지 않음:** 내부 저장 함수는 HTTP/제품 시작에 등록하지 않았다. E3 게시 접수/자동 worker/재시작 복구/공개 참조/철회/15일 만료가 다음이다. 프로세스가 원격 쓰기 도중 죽어 종료가 불확실하면 기록을 보존하고 삭제 완료를 막는다. 그 불확실 상태를 해소할 복구·운영 절차까지 연결해야 실제 기능을 개방할 수 있다. E2/E3 전체 완료가 아니다.

새 SQL migration 0013과 JSON 저장 형식 v6이 추가됐다. **Replit 반영 전 별도 migration 실행과 기존 실행 역할의 `guide_assets` 권한 검증이 필요하다.** 이번에 운영 DB/역할/키/개인 영상/실제 AI를 건드리지 않았으며 기존 MD 목표·완료 조건도 바꾸지 않았다. Replit pull/재시작/배포는 하지 않았다.

## 이전 구현 — 2026-09-20 개인정보 직접 확인 저장

E1의 문서 v2 확인 기록과 소유자 전용 GET/POST를 연결했다. 제목·단계 문구·화면의 확인을 별도로 저장하고, 해당 내용 변경/새 AI 후보/다른 프레임으로 교체되면 재확인한다. 후보 pending/masked/dismissed는 서버 후보에 묶이며 masked에는 전체를 덮는 활성 가림이 필요하다. 일반 저장으로 확인 기록을 위조하거나 v1으로 다운그레이드할 수 없다. 공개·외부 AI는 계속 차단된다. 상세는 `PRIVACY_REVIEW_CONTRACT.md`.

로컬 합성 UI에서 확인 저장·편집 후 취소·후보 판단·미리보기 연계·재접속 복원을 확인했다. 객체 속성 순서 때문에 불필요한 저장이 발생하던 문제와 320px 긴 버튼 넘침을 실제 UI에서 발견해 수정했다. 기존 개발 계획은 변경하지 않았고 Replit/Google/개인 자료는 건드리지 않았다. 시험 서버·자료·탭과 viewport는 정리했다.

최종 일반 전체 **897개 통과**(이관 99 / 서버 699 / 클라이언트 99), 실패·취소·생략 0, 종료 코드 0. 서버/화면 제품·테스트 타입 검사와 양쪽 빌드 통과. 기존 화면 소스맵·578.53 kB 번들 경고는 남는다. v2 확인 후에도 가림이 많은 초안을 편집할 수 있도록 일반 초안 JSON 상한은 1 MiB로 확장했고, 확인 명령 자체는 8 KiB·기존 인증/빈도 제한을 유지했다.

**추가 DB 검증 완료:** 사용자가 Docker 실행을 알려 기존 `artifacts/api-server/scripts/verify-postgres.mjs --local-docker`를 재개했다. 코드 `a0f4e01`에서 PostgreSQL 16.15 통합 검사 **103개 통과**, 실패·취소·생략 0, 종료 코드 0이다. 새 E1 검사 2개는 재접속 후 v2 확인 기록 복원·동시 수정 충돌·편집 시 해당 확인 취소·삭제 후 오래된 요청 거절과, 저장 실패 시 JSONB/보존 시각의 동시 롤백을 실제 DB에서 확인했다. 일반 897개는 직전 코드 검증 결과이며 이번에는 DB 검사만 추가 실행했다.

기존 로컬 `postgres:16` 이미지의 loopback 전용 일회용 컨테이너 `showme-b5-5550f9c7fe884f7f855e2a155ab0b32e`를 사용했고, 실행 후 `postgres_fixture_removed`로 해당 컨테이너와 시험 데이터 제거를 확인했다. .env·Replit DB·개인 영상·실제 외부 AI는 사용하지 않았다. E1의 로컬 코드/UI/실제 DB 검증까지 완료했으며 Replit 실환경 반영 완료는 아니다. 다음 제품 코드는 처리본 내구 저장·게시 확정(E2/E3)이며 현재 공개는 금지한다. Replit pull/재시작도 아직 하지 않았다.

## 이전 구현 — 2026-09-20 수동 가림·비공개 처리본

가림 영역 추가/이동/크기/켜기/끄기/삭제를 기존 충돌 보호 자동 저장에 연결했다. 소유자·저장 revision·미디어 지문을 확인하는 새 비공개 API가 영역 픽셀을 원본과 무관한 불투명 타일로 바꾸고 전체 PNG/썸네일을 만든다. 실패 시 원본으로 대체하지 않는다. 로컬 합성 브라우저에서 편집·저장·복원, 실제 처리본, 320px/390px 배치, 처리 실패, 다른 창 충돌 때 입력 유지/미리보기 차단을 확인했다. 임시 서버·합성 저장 폴더는 정리했다. 상세는 `PRIVACY_MASK_PREVIEW.md`.

최종 일반 전체 테스트 **872개 통과**(이관 99 / 서버 682 / 클라이언트 91), 실패·취소·생략 0, 종료 코드 0. 서버/화면의 제품·테스트 타입 검사와 양쪽 빌드도 통과했다. 기존 소스맵·번들 크기 경고는 남는다. 이 단계는 로컬 코드·검증 결과이며 Replit pull/재시작/실환경 UI 검증은 아직 수행하지 않았다.

기존 개발 계획은 변경하지 않았다. D2/E2의 부분 구현이며 개인정보 확인 완료(E1)나 게시/공유 완료가 아니다. 원본은 비공개로 유지하고 처리본은 요청 시 메모리에서만 생성한다. 다음은 후보별 확인과 화면·문구 확인 지문(E1), 이후 처리 자산 저장·게시 확정(E2/E3)이다. 일반 AI off와 공개 차단을 유지했고 이번에는 Replit/Google 데이터·키·설정을 건드리지 않았다.

## 이전 완료 지점 — 2026-09-20 고정 합성 AI 실환경 검사

새 키 원문을 열지 않고 Replit 새 셸의 설정 존재를 확인했다. 기존 비공개 접근 검사도 다시 통과했다. 실제 Replit DB/Storage와 제품 bootstrap·loopback API를 사용한 고정 합성 두 화면의 계수 1회/생성 1회가 모두 HTTP 200이었다. 입력 2,771 / 출력 314 토큰의 생성 결과가 2단계 초안 revision 1로 저장됐고, 운영 승인 철회 후 새 실행 503을 확인했다. 개인 영상과 공개 공유는 사용하지 않았다.

최초 명령은 마지막 정리에서 실패했다. 같은 시험 가이드만 대상으로 하는 `--cleanup-only`를 보완해 AI 재호출 없이 정리를 완료했다. 최종 읽기 전용 확인은 review revoked/version 2, halted true, 시험 가이드 삭제, journal 제거, count/generation 각각 settled 1, 기존 API health 200이다. 최초 실패를 소급해 단일 명령 PASS라고 보고하지 않는다. 상세와 한계는 `ANALYSIS_LIVE_ACCEPTANCE.md`.

코드 `23d945e`, `30881de`, `a2aecd9`를 기존 브랜치와 Replit에 반영했다. 기본 서버의 AI off는 변경하지 않았고 서버 재시작/공개 배포도 하지 않았다. 원래 개발 계획은 변경하지 않았다.

최종 일반 전체 테스트 **854개 통과**(이관 99 / 서버 669 / 클라이언트 86), 실패·취소·생략 0, 종료 코드 0. 새 실행기 문법 및 diff 공백 검사도 통과했다. 앞선 서버 검사 1개 실패는 상세 원인을 보존하지 못했고 제품 코드 수정 없이 재실행이 통과했으므로 원인 해결로 표시하지 않는다. 자세한 실행 구분은 `ANALYSIS_LIVE_ACCEPTANCE.md`에 남겼다.

다음 제품 단계는 원래 계획에 따른 개인정보 영구 가림이며 공개 공유·일반 개인 영상 AI 전송 허용·출시 검증도 아직 별도다. 완료한 합성 실호출이나 DB 운영자 생성을 반복하지 않는다.

## 이전 재개 지점 — 2026-09-20 사용자 키 교체 완료 확인

사용자가 `replit , .env 전부 완료 기존의 키도 지웠음`이라고 확인했다. **Replit 설정과 사용자의 `.env` 키 교체, 기존 키 삭제는 사용자 완료 보고로 기록한다.** 이를 위해 새 키 상세를 다시 열거나 비밀값을 출력하지 않는다. 기존 키 삭제를 다시 시도하지 않는다. 새 키가 실행 중 프로세스에 반영됐거나 실제 호출이 성공했다는 뜻은 아니며, 해당 검증은 아직 남아 있다.

다음은 새 설정이 반영된 제한된 시험 환경을 확인하고, 기존에 승인된 고정 합성 두 화면의 AI 실환경 검사로 복귀하는 것이다. 새 키 원문을 대화/로그/명령 인수에 남기지 않으며 개인 영상은 사용하지 않는다. 이전 운영자 계정과 최소 권한은 유지하고, 기본 API의 AI off 설정을 임의로 바꾸지 않는다. 원래 개발 계획은 변경하지 않았다.

## 이전 중단 지점 — 2026-09-20 키 폐기 요청 후

사용자가 기존 키 폐기를 요청하고 `.env` 변경 여부를 물었다. SHOW ME 프로젝트 필터의 단일 키 행에서 삭제 확인을 제출했으나 AI Studio가 `API 키를 삭제할 수 없습니다. 다시 시도해 주세요.`를 표시했다. 새로고침 후 같은 프로젝트 목록에는 2026-09-20 생성된 `Gemini API Key`가 남아 있고, 직전 턴에서 노출된 기존 키의 이름/마스킹 식별과 다르다. **삭제 전에 기존 키와의 일치 확인이 부족했다. 삭제 성공을 확인한 것이 아니며 다른 키에 재시도하지 않는다.** 이전 키가 목록에 보이지 않는 이유나 사용자의 별도 교체/삭제 여부는 아직 확인하지 않았다. 사용자에게 이를 알리고 교체 여부를 확인한다. 키 원문은 이번에 출력하지 않았고 새 키 생성/설정 변경/AI 호출도 하지 않았다.

현재 서버 시작 코드는 `process.env.GEMINI_API_KEY`를 사용하며 표준 시작 경로에서 `.env`를 자동 로드하지 않는다. Replit에서는 Secrets의 같은 이름 값을 교체해야 하며, 별도로 사용하는 로컬 `.env`에 기존 키가 있으면 그 설정도 교체해야 한다. 키 폐기가 설정 파일이나 실행 중 프로세스의 값을 자동 교체하지는 않는다. 새 인증값 입력은 사용자가 직접 하며 채팅에 붙이지 않는다. AI off 상태를 유지한다.

## 직전 중단 지점 — 2026-09-20 08:46 UTC

사용자가 합성 AI 실환경 검사 진행에 `ㄱㄱ`로 답했다. SHOW ME AI Studio의 무료 등급/결제 미설정과 모델 한도(RPM 15 / 입력 TPM 250,000 / RPD 500)를 다시 읽었다. 28일 최대 사용량을 현재 잔여량으로 사용하지 않는다.

키의 프로젝트 소속을 재확인하는 중, 브라우저 키 상세의 출력 가리기가 예상과 다른 키 형식을 처리하지 못해 **키 원문이 이 대화의 도구 출력에 한 차례 표시됐다**. 사용자에게 즉시 알리고 상세를 닫았으며 작업 변수의 원문 참조를 해제했다. 이미 반환된 도구 기록 삭제나 메모리 완전 소거를 주장하지 않는다. 키 원문·끝자리·비교값은 이 문서/코드/명령에 복사하지 않았다. 공개 유출이나 악용을 확인한 것은 아니다.

이 때문에 실제 AI 요청을 보내기 전에 멈췄다. 이번 턴에는 AI 활성화, 운영 기록 작성, Replit DB/Storage 변경, 개인 영상 접근·전송, 새 키 생성/기존 키 삭제/Secrets 교체를 하지 않았다. 키 전체 값의 Replit 일치 확인도 이번에는 완료하지 못했다. 앞선 운영자 계정 검증/저장은 유지된다.

다음은 사용자와 키 교체 범위를 확인하는 것이다. Google 공식 지침은 새 키 생성 → 앱 설정 교체 및 확인 → 기존 키 비활성화/삭제 순서를 권고한다: https://ai.google.dev/gemini-api/docs/api-key#leak-response-checklist . 키 생성/권한과 기존 키 폐기는 새 보안 변경이므로 승인 없이 수행하지 않는다. 새 인증값 입력·변경은 사용자에게 넘긴다. 새 키를 채팅에 붙이도록 요청하지 않는다. 확인 시 전체 상세/AX를 출력한 뒤 특정 접두사만 지우는 방식은 다시 사용하지 않는다.

현재 Chrome의 SHOW ME 필터가 선택된 API 키 목록 탭은 교체 안내를 위해 남긴다. 실호출을 완료했다거나 새 키 연결이 검증됐다고 보고하지 않는다. 아래 내용은 그 이전까지 완료한 상태다.

## 이전 구현·검증 기록 (고정 합성 실환경 검사 전)

이 절부터는 이전 시점의 기록이다. 최신 상태와 다음 작업 판단은 문서 맨 위의 고정 합성 실환경 완료 기록을 우선한다. 아래의 활성화·실호출 예정 항목을 다시 실행하거나 계정을 재생성하지 않는다.

### 당시 위치

- 실제 작업 저장소: `C:/Users/fxxkm/Desktop/capcha/work/showme-replit`
- 브랜치: `codex/replit-migration-hardening`
- 최신 코드 커밋: `41d6585` (`feat(showme): provision bounded analysis operator and qualify storage evidence`). 아래 기록을 추가하는 후속 문서 커밋은 별도다.
- 코드 커밋을 기존 브랜치로 푸시하고 같은 Replit 개발 프로젝트에 fast-forward 반영했다. 운영자 생성 및 재연결까지 성공했다. Replit의 실행 중 API는 재시작하지 않았으므로 새 저장소 근거 코드가 API 번들에 로드됐다고 표시하지 않는다. 운영자 CLI는 새 소스를 직접 사용했다.
- 상위 `capcha` 저장소와 그곳의 기존 수정/문서는 별도다. 오래된 상위 소스에 이 변경을 덧붙이거나 기존 변경을 되돌리지 않는다.

## 방금 완료한 코드

합성 전용 시험의 저장소 확인 근거를 명시적으로 구분했다.

- Replit 공식 정책 + 정확한 버킷/접두사 + 앱 소유자/서명/무권한 접근 검사를 근거로 사용할 때는 `replit-policy-and-app-check`, `internalPermissionsVerified:false`로 기록한다.
- 내부 접근 권한을 직접 확인한 `direct-permission-review`와 구별한다. 이번 실환경을 직접 IAM 검증 완료로 표시하지 않는다.
- 근거 누락/혼합과 옛 근거 없는 승인은 거절한다. 해당 구분이 준비 상태와 AI 전송 전 검사에도 남으며, 근거 변경 시 이전 허가가 무효화되는지 검사했다.
- 고정 합성 입력만 허용, 무료 전용, 유료 fallback 금지, 개인 영상 차단, 기존 만료·철회 조건은 유지했다.
- 승인·중지 전용 최소 권한 계정 생성 스크립트 `provision-operator.mjs`와 `operator-role-setup.ts`를 추가했다. 실제 로그인/잘못된 비밀번호 거절/유효 열 권한 검사/실패 시 새 역할 정리/중복 생성 거절을 포함한다.

관련 로컬 변경:

- `artifacts/api-server/src/processor/analysis-operations-review.ts`
- `artifacts/api-server/tests/helpers/operations-review-fixture.ts`
- `artifacts/api-server/tests/analysis-operations-review.test.ts`
- `artifacts/api-server/tests/analysis-readiness.test.ts`
- `artifacts/api-server/integration/postgres.test.ts`
- `docs/ANALYSIS_LIVE_OPERATIONS_CHECK.md`
- 이 인계 문서

## 검증 결과

- 최종 일반 전체 850개 재실행 통과: 이관 95 / 서버 669 / 클라이언트 86. 실패·취소·생략 0.
- 관련 검사 58개 별도 통과.
- API 제품/테스트 타입 검사, 서버 빌드, diff 공백 검사 통과.
- 운영자 생성 절차 추가 후 PostgreSQL 16.15 통합 검사 **101개 통과**, 실패·취소·생략 0, 종료 코드 0. 두 종류 저장소 근거의 수명주기와 최소 권한 운영자 생성/운영 CLI/실패 정리를 검사했다.
- 최신 실행의 컨테이너 `showme-b5-00054c7e4cd840fea39c75f956bcf26d`와 일회용 데이터 정리 로그를 확인했다. 이 로컬 검사의 외부 AI/Storage는 모의 응답만 사용했다. 이어진 Replit 계정 생성은 아래 실환경 기록으로 구분한다.

## Docker: 검사 재개 성공 / 이전 오류 이력

- 설치된 Docker Desktop: `4.89.0.238018`.
- 시작 시 `C:/Users/fxxkm/AppData/Local/Docker/run/sailor-ingest.sock` → `.stale` 이름 변경이 `The file cannot be accessed by the system`으로 실패했다.
- 로컬 Linux 엔진 파이프가 없어 DB 검사에 접속하지 못했다. ShowMe 테스트 assertion 실패가 아니다.
- `docker desktop start --detach`는 시작 접수를 반환했지만 엔진 정상 기동은 확인되지 않았다. 이후 사용자가 오류 화면을 제공했다.
- 사용자에게 **Quit → 작업 저장 → Windows 다시 시작 → Docker Desktop 실행**을 안내한 뒤 `docker 실행 완료` 답변을 받았다. 실제 임시 DB 생성과 전체 DB 검사 성공으로 현재 엔진 연결을 확인했다. 사용자가 수행한 세부 복구 방법이나 근본 원인 해결까지 확인한 것은 아니다.
- 파일/폴더 삭제·이름 변경, 초기화, 재설치, ACL 변경, WSL 초기화, 진단 업로드는 하지 않았다. 무작정 반복 재시작하거나 데이터를 지우지 않는다.

## 다음 순서

1. 계정 생성은 완료했다. 사용자가 권한 범위를 설명받고 `진행해`로 승인했으며 실제 SHOW-ME 개발 DB에 1개를 생성했다. **동일 계정을 재생성하거나 권한 생성 승인을 다시 묻지 않는다.** 저장된 연결을 안전하게 읽어 사용한다. 상세는 `REPLIT_OPERATOR_SETUP.md`.
2. 다음은 최신 실제 운영 관측과 키/프로젝트 바인딩을 확보하고, 고정 합성 두 화면에만 짧은 활성화를 적용하는 것이다. 실제 외부 요청은 입력량 계산 1회 + 생성 1회, 재시도/유료 전환/개인 영상 전송 없이 범위를 명시한 뒤 진행한다. 필요한 시점에 새 소스로 API를 빌드/기동하되 무의미한 반복 재시작은 하지 않는다.
3. 실제 제품 경로의 실행 → 결과 저장 → 철회/중지 및 시험 데이터 정리를 검증한다. 독립 진단 호출이나 모의 응답 성공을 제품 실환경 완료로 보고하지 않는다.

### 로컬 DB 재검사 명령

아래 스크립트는 기존 로컬 `postgres:16` 이미지만 사용하며 앱 `.env`/Replit DB/개인 영상을 사용하지 않는다. 고유 이름의 loopback 전용 임시 컨테이너를 만들고 자기 시험 데이터만 정리한다. Docker 실행 파일 접근에 실행 환경의 별도 승인이 필요할 수 있다.

```powershell
Set-Location 'C:/Users/fxxkm/Desktop/capcha/work/showme-replit'
$env:SHOWME_TEST_DOCKER_HOST='npipe:////./pipe/dockerDesktopLinuxEngine'
$env:SHOWME_TEST_DOCKER_BIN='C:/Users/fxxkm/AppData/Local/Programs/DockerDesktop/resources/bin/docker.exe'
$env:SHOWME_TEST_FFMPEG_PATH='C:/Users/fxxkm/Desktop/capcha/node_modules/ffmpeg-static/ffmpeg.exe'
$env:SHOWME_TEST_FFPROBE_PATH='C:/Users/fxxkm/Desktop/capcha/node_modules/ffprobe-static/bin/win32/x64/ffprobe.exe'
node artifacts/api-server/scripts/verify-postgres.mjs --local-docker
```

## 실환경의 마지막 확인 상태

- Replit: `companynumeo/SHOW-ME`, 프로젝트 `67fdf570-63d0-47d4-a842-742d022f2eb9`.
- 실행용 DB 로그인 분리와 verify-only migration 시작은 이전 단계에 적용/검증했다. 상세: `REPLIT_RUNTIME_ROLLOUT.md`.
- 생성 전 운영자 로그인 0개를 확인하고 `showme_analysis_operator_dev_d10d4afd09a33e52` 1개를 생성했다. 연결은 Git 제외 `.local/showme/operator-db.json`, kind `showme-development-operator-v1`에만 저장했다. 비밀번호/URL은 출력하지 않았다.
- 별도 프로세스에서 저장 파일을 안전하게 다시 읽어 접속·권한 검사에 성공했다. 가이드/단계/초안의 `SELECT * ... LIMIT 0` 세 쿼리가 모두 `42501`로 거절됐다. 내용은 조회하지 않았다. 승인/활성화 이력은 여전히 0개다.
- 운영 CLI `status` 성공: `state:missing`, `version:0`, `lastActivationVersion:0`, `authorizesAnalysis:false`. 전역 제어의 `halted:false`는 기존 값이며 이를 승인 완료나 활성화라고 해석하지 않는다. 이번에 운영 기록/제어 값을 쓰지 않았다.
- 실제 API 자식 1개의 환경 검사: AI off, AI 키/운영자 URL 없음, 기존 runtime 로그인 및 verify-only 유지. `/api/healthz` HTTP 200, `status:ok`. API 재시작·공개 배포는 하지 않았다.
- AI Studio `SHOW ME`: `gen-lang-client-0316998263`. 무료/결제 미설정, 시험 모델 한도 RPM 15 / 입력 TPM 250,000 / RPD 500을 9월 20일 UI에서 확인했다. 실제 승인 기록에는 새 관측 시각/근거가 필요하며, 인계 문서의 날짜를 대신 넣지 않는다.
- 사용자는 시험 중 이 AI 프로젝트를 다른 곳에서 사용하지 않는다고 답했다.
- 서버 키의 프로젝트 일치 근거는 9월 18일 기록이며 이번에 갱신하지 않았다. 다음 실제 활성화 전에 확인한다. 비밀값 원문을 문서·로그·명령 인수에 남기지 않는다.
- 이번 재개는 코드 반영과 승인된 DB 운영자 1개 생성까지 완료했다. 실제 Google 호출·실환경 AI 활성화·개인 영상 외부 전송·공개 배포·과금 변경은 하지 않았다.

추가 근거 및 한계: `ANALYSIS_LIVE_OPERATIONS_CHECK.md`, `ANALYSIS_EXPLICIT_ACTIVATION.md`, `ANALYSIS_REPLIT_BOOTSTRAP.md`, `REPLIT_RUNTIME_ROLLOUT.md`.
