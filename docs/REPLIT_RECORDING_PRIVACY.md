# 녹화·비공개 전송 경계 — 2026-09-17

사용자 기준: **Replit 처리·저장은 허용. 원본 영상·추출 이미지·접근 키를 별도 서버, 외부 AI, 공개 공유로 보내지 않는다.**

이 변경은 앱 코드의 방어 보완이다. Replit 실환경 권한 감사, 브라우저/운영체제 전체 보안 감사, 유출 가능성 0% 보증이 아니다. 실제 개인 화면은 검사에 사용하지 않았다.

## 파일의 경로

1. 화면 선택은 브라우저 권한 창을 사용한다. 오디오는 요청하지 않으며 예상 밖 오디오 트랙도 거부한다.
2. 녹화 중 조각과 완료한 파일은 브라우저 메모리의 Blob/File로만 다룬다. 녹화 완료·파일 선택만으로 업로드하지 않는다. 녹화 코드에는 네트워크·영구 저장·다운로드 API가 없다.
3. 사용자가 **장면 추출 시작**을 눌러야 같은 앱 주소의 `/api/guides`로 원본을 전송한다. Replit에서는 기존 `/api` 프록시 → API Server → Replit App Storage를 사용한다. 서버는 처리 중 임시 로컬 사본도 사용한다.
4. 원본과 추출 화면은 Replit 저장소에, 메타데이터·편집 초안은 기존 DB에 보관한다. 기존 전체 작업 삭제와 미공개 초안 보관 만료 정리 절차를 유지했다. 메모리에서 참조를 지우는 것은 장치의 물리적 완전 삭제를 보장하지 않는다.

Replit App Storage 자체는 Google Cloud Storage 기반이다. 이는 Replit 저장 서비스의 기반 인프라이며 Gemini 등 외부 AI 전송과 다르다. [Replit 공식 App Storage 문서](https://docs.replit.com/features/data-and-storage/object-storage).

## 추가한 방어

- 업로드/상태/초안 저장/재시도/삭제를 모두 같은 출처(origin)로 제한한다. 환경 설정이나 복구 기록에 다른 호스트·포트·HTTP 강등·인증정보 포함 URL이 있으면 파일/키를 보내기 전에 거부한다. 복구 키를 임의로 삭제하지 않는다.
- XHR 업로드를 `fetch` + `redirect: error`로 변경했다. 응답 후 주소를 확인하는 방식으로는 이미 전송된 본문을 되돌릴 수 없기 때문이다. 같은 출처로의 리다이렉트도 거부한다. 재시도 요청에도 같은 제한을 적용한다. [RequestInit 공식 참조](https://developer.mozilla.org/en-US/docs/Web/API/RequestInit).
- Fetch에는 이 앱에서 쓸 수 있는 범용 업로드 진행 이벤트가 없어, 업로드 중 가짜 백분율 대신 서버 접수 응답을 기다린다고 표시한다. 영상 처리 단계의 서버 진행률은 유지한다.
- 프런트엔드 CSP로 연결·스크립트·이미지의 외부 주소를 제한하고, 외부 폰트 로드 및 선택적 Replit 개발 DOM/오류 브리지를 제거했다. React/Vite 개발 호환을 위해 인라인 스크립트·스타일 허용은 남아 있으므로 완전한 XSS 방어라고 주장하지 않는다. 호스팅 프록시가 별도로 주입하는 코드까지 제어한다는 뜻도 아니다.
- 추출 이미지/썸네일 URL은 해당 가이드·단계의 같은 출처 자산 경로인지 검사한다. 앱 API/이미지 응답은 `no-store`, `no-referrer`, `nosniff`, `Cross-Origin-Resource-Policy: same-origin`을 사용한다. 과거에 저장된 캐시나 이미 복사한 파일까지 소급 삭제하지는 않는다.
- 활성 업로드/처리/정리/시작·종료/작업 큐의 오류 로그는 이벤트·안전한 코드만 유지하고 SDK/FFmpeg/DB의 원문 예외 문자열을 출력하지 않는다. 호스팅 프록시·클라우드 사업자의 자체 로그는 별도다.
- AI admission의 기존 기본 비활성 상태를 유지한다. AI runner나 외부 provider를 앱 시작 경로에 연결하지 않았다. 이후 외부 AI 연동은 이번 허용 범위에 포함되지 않는다.

## 캡처 수명 관리

- 녹화 종료 버튼에서 MediaRecorder 종료 요청과 함께 **모든 MediaStreamTrack을 즉시 stop**한다. 비동기 `onstop`까지 캡처를 계속 유지하지 않는다. [MediaRecorder.stop](https://developer.mozilla.org/en-US/docs/Web/API/MediaRecorder/stop), [MediaStreamTrack.stop](https://developer.mozilla.org/en-US/docs/Web/API/MediaStreamTrack/stop).
- 취소·페이지 이탈·컴포넌트 해제·레코더 오류·크기 초과에서 캡처를 중단하고 조각/타이머/이벤트 참조를 정리한다. 늦게 도착한 화면 선택·종료 이벤트가 새 파일을 넘기지 못하게 세대 식별자로 차단한다.
- `녹화 취소·버리기`와 업로드 전 `선택한 영상 비우기`를 제공한다. 비우기는 선택된 메모리 참조를 버리는 동작이며 사용자의 원래 디스크 파일을 삭제하지 않는다.
- 정상 종료 후 마지막 데이터가 오지 않으면 3초 후 파일을 버리고 실패로 알린다. 빈 파일은 성공으로 처리하지 않는다. 브라우저 종료/OS 정지/프로세스 손상까지 앱 코드가 통제한다는 뜻은 아니다. 4분 제한도 브라우저 이벤트가 실행될 때 적용되며 실시간 OS 보장은 아니다.

## 검증 및 아직 남은 확인

### 현재 진행 체크포인트 — 2026-09-17 / ccc50d7

사용자가 제공한 출력·화면·완료 보고와 로컬 검증을 구분한다. 아래 기존 615개 기록은 이전 로컬 검증 이력이다.

- [x] Replit에서 `ccc50d7` 반영 후 일반 전체 검사 629개(64+508+57), 실패·생략 0 및 전체 타입/빌드 성공 로그 확인. 첫 무관찰 실제 JPEG 검사 약 6.27초 통과, 손상 JPEG 거부·종료/취소 검사 유지. 한 번의 실행 증거이며 장기 OS 지연 해소를 보장하지 않는다.
- [x] 기존 워크플로 재시작 뒤 FFmpeg/FFprobe 8.1.2 검증 및 `processor_started`(storage=replit/repository=postgres) 화면 확인.
- [x] 합성 영상의 편집·자동 저장 화면 확인 및 사용자 테스트 작업 삭제 완료 보고 수신. 녹화나 외부 전송 부재의 증거로 확대하지 않는다.
- [x] 정상 녹화 종료의 UI 확인: 사용자 YouTube 탭 녹화 후 1.3 MB WebM의 `업로드 준비 완료` 및 `화면 녹화` 대기 버튼 복귀를 확인했다. 사용자는 브라우저 공유 표시가 외관상 사라졌다고 보고했고 후속 전체 브라우저 사진에서도 공유 표시는 보이지 않는다. 육안/사용자 보고 범위이며 네이티브 트랙 상태·영상 내용·무전송을 직접 검증한 것은 아니다. 이 녹화물에 개인정보가 없다고 가정하거나 업로드를 요구하지 않는다.
- [x] 녹화 완료 후 파일 선택 해제: 사용자가 버튼 사진으로 **녹화를 끝낸 뒤 `선택한 영상 비우기`를 눌렀다**고 명확히 했다. 이는 업로드 전 선택 해제의 사용자 보고이며 녹화 중 취소나 서버 삭제가 아니다. 앞서 공유 표시 종료를 취소 동작의 증거로 기록한 판단을 취소하고 정상 종료의 증거로만 유지한다.
- [x] 녹화 중 앱의 `녹화 취소·버리기` 후 공유 표시 종료·새 파일 미생성: 정상 종료/파일 비우기와 구분한 절차를 안내한 뒤 사용자가 `저렇게 다 한거야`라고 완료를 확인했다. 사용자 실기기 보고로 기록하며 같은 검사를 다시 요청하지 않는다. 네이티브 트랙/네트워크 직접 관찰의 증거로 확대하지 않는다.
- [x] 입력 유지 정책 확정: 사용자는 잘못 고른 영상을 바꿀 때 작성한 답변까지 사라지는 것을 원하지 않아 현행 동작 유지를 선택했다. 영상 교체/`선택한 영상 비우기`에서 제작 의도의 세 답변을 초기화하지 않는다. `영상과 입력내용 모두 비우기` 제안은 채택하지 않았으며 제품 코드를 변경하지 않았다. 이는 정책 결정이지 아직 관찰하지 않은 UI/전송 검사의 완료 표시가 아니다.
- [x] 브라우저의 공유 중지 경로: 앱 녹화 상태 종료·정상 파일 준비를 확인하도록 안내한 뒤 사용자가 `공유 중지도 되더라`라고 동작을 보고했다. 사용자 UI 확인으로 기록하며 네이티브 트랙/전송 경로를 직접 관찰한 것으로 확대하지 않는다.
- [x] 녹화 중 ShowMe Preview 새로고침: 안내 뒤 사용자가 `니가 말한대로 됐네 녹화중에 재시작 누르니까 녹화 꺼지고`라고 보고했고, 사진에서 녹화 대기 버튼·파일 없는 초기 화면·비활성 추출 시작 버튼을 확인했다. 이 새로고침 경로의 사용자/화면 확인이며 모든 페이지 이탈·브라우저 종료·OS 장애 경우를 검사한 것으로 확대하지 않는다.
- [x] 파일 선택 후 새 요청 행 관찰: 사용자 Chrome의 ShowMe 단독 탭에서 새로고침 시 JS/CSS 요청이 나타남을 확인한 뒤 목록을 비웠다. 합성 `showme-synthetic-3-scenes.mp4`가 `업로드 준비 완료`로 선택된 화면과, 기록 중·All·빈 필터 상태의 빈 Network 목록을 함께 확인했다. 하단에 이전 67건 집계가 남아 있어 이를 0건으로 기록하지 않는다. 이 파일 선택 관찰에서 새 요청 행이 보이지 않았다는 근거이며, 기존 WebSocket 프레임·브라우저 전체·서버 측 무전송을 직접 검사한 것은 아니다.
- [x] 기존 초안 조회 목적지: 사용자가 `draft` 요청의 General/Request URL을 현재 ShowMe 주소와 비교한 뒤 `같네`라고 보고했다. HTTPS와 같은 도메인의 초안 조회를 사용자 확인으로 기록한다. URL 전체·키·쿠키·본문은 수집하지 않았다. 업로드 POST의 목적지나 모든 서버 송신이 확인된 것은 아니다.
- [x] 현재 소스의 서버 시작 경로 재확인: `src/index.ts` → `processor/index.ts`의 `startProcessor`는 장면 추출용 `DurableProcessingDispatcher`를 시작하며 AI 분석 dispatcher/provider를 연결하지 않는다. `server.ts`의 기본 `DurableAnalysisAdmission({ repository })`에는 readiness가 없어 신규 분석 접수가 거부된다. 기존 예약 재조회 경로는 있지만 AI 실행기를 시작하지 않는다. `storage.ts`는 `storageDriver=replit`일 때 `@replit/object-storage` Client를 사용하며, 앞서 사용자 기동 로그가 이 선택을 보여 주었다. 이는 `ccc50d7` 소스 경로와 제공된 로그의 확인이지 실행 중 전체 송신·SDK 내부 동작·실제 저장소 ACL의 감사는 아니다.
- [ ] 실제 녹화 중/완료 전후의 업로드 전 통신 확인. 기존 녹화 종료 UI 검사만으로 무전송까지 확인한 것으로 처리하지 않는다.
- [x] 2026-09-18 프로젝트 접근 목록: Replit 편집기의 Invite에서 본인 외 사람·그룹이 있는지 확인하도록 안내했고 사용자가 `나만있어`라고 보고했다. 표시된 프로젝트 접근 목록의 사용자 확인이며 Storage/DB의 실제 권한, 워크스페이스 관리자·서비스 계정, 게시 앱 접근 또는 공개 링크까지 검증한 것은 아니다. 권한을 변경하거나 초대하지 않았다.
- [ ] 명시적 합성 원본 업로드 POST의 실제 목적지: 사용자 화면에서 `합성 영상 확인` 제목·3개 장면·서버 자동 저장 표시, Network의 가이드/초안 조회와 이미지 200 응답을 확인했다. 그러나 `method:POST` 필터 결과는 사용자가 `없음`이라고 보고했다. 현재 기록에서 POST를 확인하지 못한 것이며 누락 원인·새 업로드 여부를 이 자료만으로 확정하지 않는다. 조회 성공이나 코드의 동일 출처 제한을 업로드 POST의 실환경 관찰을 대체하는 근거로 사용하지 않는다.
- [ ] Replit 프로젝트/Storage/DB 접근 권한, 실제 HTTPS·프록시/주입 코드·CSP, 사업자 로그/백업 보관 정책 확인. 앱 코드의 방어와 플랫폼의 운영 권한을 구분한다.

2026-09-18 App Storage 화면 결과: 사용자는 Settings에 `버킷 아이디 하나만 보여`라고 보고했다. 버킷 ID 값은 수집하지 않았고 존재하지 않는 권한 메뉴 탐색을 반복하지 않는다. [현재 영문 공식 문서](https://docs.replit.com/features/data-and-storage/object-storage)는 Settings를 버킷 ID 확인 위치로 설명하며, 버킷이 프로젝트 전용이라고 안내한다. 이것만으로 해당 실행 환경의 실제 IAM/객체 공개 권한을 검증했다고 판정하지 않는다. 설치된 `@replit/object-storage` 1.0.0의 공개 Client 선언도 파일 작업만 제공하며 IAM 조회 메서드는 확인되지 않았다. 내부 인증정보를 꺼내거나 숨은 SDK 메서드를 호출해 우회하지 않는다. 저장소 권한 항목은 별도 운영 확인이 필요하며 미확인으로 유지한다. 버킷 생성·삭제·연결 변경이나 실제 SDK 호출은 하지 않았다.

2026-09-18 사용자는 ShowMe가 업로드 초기 화면이라고 보고했다. 초기 화면만으로 기존 서버 작업이 삭제됐다고 가정하지 않는다. 사용자가 합성 자료 자동검사 준비에 `진행`을 승인했으므로, 기존 가이드/개인 녹화물 없이 실행 가능한 [합성 접근 자동검사](REPLIT_PRIVATE_ACCESS_CHECK.md)를 추가했다. 고정된 합성 자료로 새 시험 ID/키를 만들고, 정상 가이드·초안·이미지 조회를 기준으로 무키/잘못된 키 거부와 삭제를 검사한다. 정리 실패는 PASS로 숨기지 않으며 전용 비공개 복구 기록으로 삭제만 다시 시도할 수 있다. 임의·삭제된 ID의 404는 성공으로 세지 않는다. Node의 직접 API 검사이며 브라우저 무전송/업로드 목적지나 저장소 IAM 감사의 대체가 아니다. 실제 Replit에서는 아직 실행하지 않았다.

자동검사 준비 검증: 로컬 단위/HTTP/CLI 26개, 실제 HTTP/FFmpeg/LocalStorage 통합 2개, 테스트 TypeScript·스크립트 구문 검사가 통과했다. 일반 전체 회귀 `node scripts/run-tests.mjs`도 migration/server/client를 순서대로 마치고 종료 코드 0으로 통과했다. 복구 파일 Git 제외 검사를 추가했고 미확인 삭제/실패를 PASS로 바꾸지 않는 것을 검사했다. 이번 변경은 진단·테스트·문서·복구 파일 제외 규칙에 한정하며 제품 소스·DB 스키마·의존성·Replit 설정은 바꾸지 않았다. 준비 완료 뒤 사용자가 검사 파일의 GitHub 커밋·푸시에 `ㅇ`으로 승인했다. 반영 대상은 기존 `codex/replit-migration-hardening` 브랜치이며 main 병합·Publish·Replit 실행 승인을 대신하지 않는다. 실제 Replit 자동검사 실행 결과는 아직 없다.

현재 확인된 동작은 녹화 정상 종료, 그 뒤 파일 선택 해제, 녹화 중 앱 취소다. 정상 종료 뒤 2.9 MB WebM 준비 화면도 받았으며 함께 첨부된 브라우저 공유 배너 사진은 사용자가 종료 전 사진이라고 명확히 했다. 이를 종료 후 공유 잔류 오류로 해석하지 않는다. 코드상 `녹화 취소·버리기`는 ScreenCapture 세션만 취소하며 이전에 선택된 UploadScreen 파일과 제작 의도를 초기화하지 않는다. `선택한 영상 비우기`는 파일과 파일 input을 비우지만 intent/intentError 상태를 지우지 않는다. 질문 영역은 `file`이 있을 때만 렌더링하므로 입력 상태 잔류와 질문 영역 표시를 구분한다. 사용자가 영상 교체 시 답변 유지를 원한다고 확정했으므로 초기화 변경은 하지 않는다.

우클릭으로 Method/Domain 열을 켜는 방법은 사용자가 작동하지 않는다고 보고해 중단했다. 대신 기존 `draft` 조회의 HTTPS·동일 도메인 비교를 사용자 보고로 마쳤다. 같은 조작·재업로드·이미 확인한 녹화 종료 검사를 반복 요구하지 않는다. 사용자 조작은 여기서 잠시 멈추며, 미확인 항목을 완료로 바꾸지 않는다. 이후 검증 범위는 원본 업로드 POST/녹화 전후 통신과 Replit 운영 접근 권한이며, 실제 민감한 영상은 사용하지 않는다. URL 전체·Request Headers·쿠키·본문·서명 URL·HAR 제출을 요구하지 않는다. 도구에는 사용자 Chrome이 연결되어 있지 않아 직접 실시간 관찰한 것으로 기록하지 않는다. 기존 WebSocket 프레임, 서버 측 송신·Storage/DB 권한·백업도 여전히 별도 확인 범위다. 이번에는 소스 읽기와 문서 갱신만 했으며 제품 코드·설정·권한·배포를 변경하거나 클라우드/AI 호출을 실행하지 않았다.

### 이전 로컬 검증 이력

- Windows / Node 24.15.0 전체 회귀: migration 64 + server 494 + client 57, **615 통과 / 실패·생략 0**. 앱과 테스트 TypeScript 검사 통과. 기존 로컬 FFmpeg 6.1.1/FFprobe 4.0.2를 테스트 전용 경로로 지정했다. 운영/Replit의 pinned 8.1.2나 보안 하한을 바꾸지 않았다.
- 실제 headless Chrome MediaRecorder에 합성 canvas 스트림을 공급했다. 실제 개인 화면/권한 창은 캡처하지 않았다. 생성된 WebM의 브라우저 디코딩까지 확인했다. 처음 매우 짧은 녹화는 인코더 준비 전에 끝나 빈 결과로 거부됐으며, 유효 파일 검사는 실제 인코딩 데이터가 나온 뒤 진행했다. 검사 통과를 위해 앱의 종료 제한·오류 처리를 완화하지 않았다.
- 종료·취소·pagehide·컴포넌트 해제·늦은 화면 선택 뒤 native track 종료, 업로드 전 POST 0, 원본 업로드의 실제 307 리다이렉트 대상 요청 0(같은 출처/다른 출처 모두), 외부 주소 복구 기록 차단, CSP 외부 요청 차단을 확인했다. 외부 네트워크·AI 호출·페이지 오류 0.
- 기존 실제 로컬 HTTP + JSON 저장소 + FFmpeg의 업로드→장면 추출→자동 저장→새로고침→충돌→삭제 브라우저 검사도 통과했다. 실제 Replit/Postgres/Object Storage를 사용한 검사는 아니다.
- production 프런트엔드 빌드와 개발 Vite/React/HMR/CSP 호환 검사 통과. 기존 소스맵 및 500 kB 이상 번들 경고는 숨기지 않았다.

아직 확인하지 않은 항목:

- Replit 프로젝트/Storage/DB의 실제 접근 권한, 공동 작업자, 서비스 계정, 실제 배포의 HTTPS·프록시/주입 스크립트·CSP 헤더, 사업자 로그/백업/복구 보관 정책.
- 앱은 종단간 암호화가 아니다. 처리 서버는 원본을 읽을 수 있고, 운영 계정/저장소 권한을 가진 주체의 접근은 별도 관리가 필요하다. 브라우저의 편집 키와 만료되는 서명 자산 URL도 자격증명이므로 공유하면 안 된다.
- 이미 내려받았거나 복사된 데이터, 브라우저 확장 프로그램, 감염된 기기, 유출된 운영 키까지 이 패치로 회수/차단할 수는 없다.

이 확인 전에는 민감한 실제 개인 화면으로 안전성을 시험하지 않는다. Replit 검증은 기존 합성 영상만 사용하고, 키·쿠키·서명 URL·DB 주소를 콘솔 출력이나 채팅으로 전달하지 않는다.

## 원격 반영 승인 후 Replit 적용

1. 올바른 개발 브랜치와 깨끗한 작업 상태를 확인하고 `git pull --ff-only`.
2. `node scripts/run-tests.mjs` 및 기존 `PORT=20116 BASE_PATH=/ pnpm run build`로 검사한다. 실패 검사를 건너뛰거나 FFmpeg 버전 검증을 낮추지 않는다.
3. 이번에는 웹과 API 코드가 모두 바뀌므로 기존 **web / API Server Workflow를 각각 갱신**한다. Shell에서 두 번째 API 서버를 띄우지 않는다. `processor_started` 이후 새로고침한다.
4. 같은 출처가 아닌 과거 `VITE_SHOWME_PROCESSOR_URL` 설정은 이제 차단된다. Replit에서는 같은 앱의 `/api` 프록시를 사용한다. 잘못된 복구 주소를 무시하며 다른 곳으로 키를 보내거나 복구 기록을 자동 삭제하지 않는다.
5. 합성 영상으로 선택만 했을 때 업로드 없음, 명시적 시작 뒤 추출·저장·삭제를 확인한다. 실제 녹화 권한 창/브라우저 공유 표시의 종료 확인도 민감하지 않은 테스트 창으로 별도 진행한다.

main 병합·Publish·실제 파일 전송·저장소 권한 변경은 이 로컬 검증에 포함되지 않는다.
