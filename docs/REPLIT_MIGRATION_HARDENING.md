# Replit 이관 안전장치·테스트 복구 — 1–2단계

기준: `f8844eb` / `codex/replit-migration-checkpoint`, 작업일: 2026-09-16.

## 변경 범위

**2026-09-17 최신 변경:** Replit `00d54a4`에서 페이지 대기 상태의 실제 FFmpeg 정체와 이미지 1개 실패를 확인한 뒤, 같은 Nixpkgs 커밋·FFmpeg 8.1.2의 `full` 의존성을 `headless`로 교체했다. 로더/기존 이미지 검사/시간 제한은 그대로다. [수정 근거와 한 번 실행할 확인 절차](REPLIT_HEADLESS_MEDIA.md). 아직 Replit headless 성능/호환 검증 전이므로 해결 완료가 아니다.

- `scripts/post-merge.sh`에서 scaffold DB의 `push` 호출을 제거했다. 기존 잠금 파일 기반 의존성 설치는 유지한다. 실제 ShowMe SQL migration이나 서버 시작 코드는 바꾸지 않는다.
- `.gitignore`에서 실제 환경 파일·인증 키·로컬 영상/프레임 데이터·도구 상태 제외를 복구했다. `.env.example`은 추적 가능하게 유지한다. 이미 추적 중인 파일은 삭제하거나 추적 해제하지 않는다.
- `.gitattributes`에서 셸 스크립트를 LF로 고정했다. Windows에서 편집 후 Linux의 후처리 실행이 깨지는 것을 방지한다.
- `test:migration`을 추가했다. Node와 Git만 사용하는 읽기 전용 회귀 검사다. 서버 import, `.env` 로딩, 의존성 설치, 후처리 실행, DB/Storage/Gemini 접속은 하지 않는다.

## 1단계 검증 기록

1단계 당시에는 저장소 루트에서 `node --test scripts/tests/migration-safety.test.mjs`를 실행했다. 현재 전체 안전 검사는 `node scripts/run-tests.mjs migration` 또는 `pnpm run test:migration`으로 실행한다. 당시 이 PC의 번들 pnpm 실행기는 설치를 자동 시도했으나 네트워크 권한 오류로 진행되지 않아 중단했고 직접 Node 실행으로 검증했다. 후속 Linux 검증은 아래 2단계 기록을 따른다.

검사 범위: 후처리 명령 제한, 위험 명령 삽입 시 검사 실패, 셸 줄바꿈, 테스트 명령 연결, 가상 비밀/출력 경로의 추적 제외, 예시/소스/SQL 경로의 추적 허용, 기존 미디어 보안 검사 유지, 실제 실행 위치의 과거 SQL/메타데이터 보존. 새 migration 추가는 허용하되 과거 journal 항목의 변경/삭제는 허용하지 않는다.

기존 의존성 설치나 서버 시작을 실행한 검증은 아니다. 이미 추적된 비밀정보를 지우는 기능도 아니며, 비밀정보 전체 감사와 구별한다.

검증 기록: Node 24.15.0 / Windows에서 변경 전 검사 실패로 누락을 재현했고, 보완 후 **42개 통과 / 실패 0 / 생략 0**을 확인했다. 셸 스크립트의 `bash -n` 구문 검사와 `git diff --check`도 통과했다. Replit/Linux에서의 후처리 실제 실행이나 전체 제품 빌드는 이번 범위가 아니다.

## 1단계 당시 다음 순서

1. 보존된 서버 단위 테스트 36개 파일·보조 자료와 클라이언트 테스트를 현재 `artifacts/` 소스를 대상으로 복구한다. 백업 소스에 대한 검사로 대체하지 않는다.
2. 테스트 경로·스크립트·개발 의존성과 별도 PostgreSQL 시험 경계를 복구하고 타입/빌드/기능 회귀 검사를 수행한다.
3. 현 잠금 파일에는 Linux 전용 네이티브 의존성 제외 설정이 있다. Windows 기존 프로젝트에 무작정 설치하지 말고 독립 환경에서 검증한다.

이번 단계에는 Replit 파일 직접 변경, main 병합, Publish, 실제 DB 변경, 저장소/Gemini 호출 또는 자동 분석 활성화가 포함되지 않는다. 로컬 보완본은 Replit에 별도로 반영해야 한다.

## 2단계 완료 — 2026-09-16

- 백업의 서버 테스트 36개 파일·보조 자료와 클라이언트 테스트를 실제 `artifacts/` 소스로 연결했다. PostgreSQL 통합 검사도 활성 코드 경로로 복구했지만 기본 실행과 분리했다.
- 그룹별/전체 테스트 실행기, 직접 테스트 의존성, 엄격한 테스트 타입 검사와 잠금 파일을 복구했다. 비밀 환경·Node 사전 로딩 옵션은 자식 프로세스에서 제외한다.
- `/api/healthz`의 준비 전 503→완료 후 200, 실행 위치별 SQL 폴더 선택, 테스트 복구 누락, 환경 제외, 미디어 도구 선택을 추가 검사한다. SQL 자체나 DB 스키마는 변경하지 않았다.
- 기존 합성 이미지의 JPEG 바이트가 OS/도구에 따라 달라져 승인 지문 비교가 실패했다. 원래 Windows 코드에서 역사적 전체 요청 지문 일치를 확인한 두 이미지를 고정했다. 참조 지문·전송 승인 조건은 변경하지 않았다. 합성 자료는 이제 설치 스크립트나 FFmpeg에 의존하지 않으며 해시 검증과 버퍼 독립성 검사도 한다.
- 기본 480개에 이관 회귀 7개와 안전 검사 49개를 더해 **536개 통과 / 실패 0 / 생략 0**이다: 서버 483, 클라이언트 4, 안전장치 49.
- 전체 패키지 타입 검사(복구된 테스트 포함), API·ShowMe·mockup 빌드, 잠금 파일 오프라인 고정 설치, 셸/실행기 구문과 Git 공백 검사도 통과했다.

환경: 공식 Node `24.15.0-bookworm-slim` 이미지(`sha256:4e6b70dd6cbfc88c8157ba19aa3d9f9cce6ba4703576d55459e45efcbc9c5f5d`), pnpm 10.17.1. 의존성 준비 후 컨테이너 네트워크를 분리해 검사했다. 운영 설정/키, 실제 DB/Storage/Google은 사용하지 않았다. 임시 환경에 고정 개발 패키지의 FFmpeg 설치만 명시적으로 수행했으며, 프로젝트의 pnpm 설치 스크립트 허용 목록은 변경하지 않았다.

합성 영상은 Linux 번들 FFmpeg 7.0.2 / FFprobe 4.0.2로 검증했다. 오래된 기본 FFmpeg 5.1.9로 실행한 최초 회전 영상 검사는 실패했으며, 위 536개는 도구 준비와 고정 이미지 보완 후 전체 재실행 결과다. 이는 Replit 운영용 8.1.2의 실환경 검사 대체가 아니다. 기존 운영 미디어 보안 기준은 그대로다.

남은 경고: ShowMe UI 세 파일의 소스맵 위치 경고와 API 번들 크기 안내가 있으나 빌드는 종료 코드 0이다. 별도 PostgreSQL 43개·Replit 브라우저 실사용·운영 보안/배포는 이번에 재검증하지 않았다.

실행 방법과 경계는 [테스트 안내](TESTING.md)에 기록했다. 전체 로컬 로그는 Git 제외 경로 `outputs/migration-verification/tests-linux.log`, `build-linux.log`에 있다. 2단계 종료 시점에는 GitHub/Replit에 반영하지 않았으며 후속 순서는 변경 리뷰·체크포인트 반영 후 Replit 합성 영상 실환경 검증이다.

## 3단계 변경 리뷰 — 2026-09-16

- 기능 코드는 위 Linux 검증본에서 변경하지 않았다. 테스트 경로 변환 외 변경이 있는 기존 파일은 준비 상태 검사, 버퍼 독립성 검사, 미디어 도구 선택을 추가한 5개이며 원래 검사를 삭제하지 않았다.
- 안전 검사 49개, 실행기 구문 및 Git 공백 검사를 다시 통과했다. 전체 536개/타입/빌드 결과는 위 2단계의 동일 기능 코드 검증 기록이다. 이 단계에서 전체 실행을 새로 했다고 계산하지 않는다.
- DB 후처리 차단, 운영 미디어 버전 검사, 과거 합성 요청 지문과 전송 승인 조건을 유지했다. 이번 리뷰에서 업로드를 막을 추가 결함은 확인하지 못했다. 이는 전체 제품 보안 인증이나 운영 승인이라는 뜻이 아니다.
- `codex/replit-migration-hardening`을 별도 보관 브랜치로 사용한다. `main` 및 기존 `codex/replit-migration-checkpoint`는 변경하지 않는다.
- Replit의 사용자 변경부터 확인하고 가져오기·재검증을 진행한다. [반영 순서](REPLIT_UPDATE.md)를 따르며, 원격 업로드 성공 여부는 실제 원격 커밋 확인으로 판정한다.

## 4단계 Replit 재검증 중 테스트 경로 보완 — 2026-09-16

- 사용자가 Replit에서 `11589a0`으로 전환하고 `pnpm install --frozen-lockfile --ignore-scripts`를 완료했다. 이후 `analysis-images.test.ts`의 재확인 횟수 검사에서 `actual: 1 / expected: 2`가 발생했고, 번들 FFmpeg 파일은 없음을 확인했다.
- 원인: 이 파일만 공통 `testMediaPaths()`를 사용하지 않고 `ffmpeg-static` 경로를 직접 참조했다. 이미지 디코딩에서 실패해 두 번째 가이드 조회까지 도달하지 못했다. 이전 Linux 검증은 번들 실행 파일을 별도로 설치해 이 누락을 발견하지 못했다.
- 해당 테스트도 공통 경로 선택기로 통일했다. 모든 미디어 테스트가 같은 선택기를 사용하고 플랫폼 보조 코드 밖에서 번들 경로를 직접 가져오지 못하도록 회귀 검사를 추가했다. 기대값·운영 코드·보안 검사·잠금 파일·설치 스크립트 정책은 바꾸지 않았다.
- 번들 모듈이 존재하지 않는 실행 경로를 반환하도록 시험 프로세스에서만 바꾸고 별도 테스트 도구 경로를 제공했다. 수정 전 같은 `1 !== 2` 오류를 재현했으며, 수정 후 해당 이미지 검사 **20개 통과 / 실패·생략 0**을 확인했다. 실제 설치 파일은 삭제하거나 수정하지 않았다.
- Windows / Node 24.15.0의 기존 로컬 의존성으로 기본 검사 **537개(안전 50 + 서버 483 + 클라이언트 4), 실패·생략 0**과 서버 테스트의 엄격한 타입 검사를 통과했다. 이는 Replit 잠금 의존성·호스트 FFmpeg 8.1.2의 재검증을 대신하지 않는다. 이번에는 전체 제품 빌드를 재실행하지 않았다.
- 앱 환경변수와 `.env`를 사용하지 않고 합성 자료·로컬 파일/HTTP·모의 공급자로 검사했다. 실제 DB/Storage/Gemini 호출, main 병합, 배포는 하지 않았다. Replit에서는 수정본을 받은 뒤 전체 테스트를 다시 확인하고, 통과해야 빌드 단계로 넘어간다.

## 5단계 Replit 이미지 읽기 지연 — 원인 분리 진행 중

- 경로 보완 후 Replit에서는 정상 이미지 읽기가 약 4.5초 제한에서 실패했다. 첫 합성 이미지만 직접 디코딩한 검사는 171ms/156ms였지만, 전체 검사 재실행과 이미지 파일 단독 검사에서도 첫 정상 읽기 검사가 실패했다. 병렬 부하만으로 설명할 수 없으며, 직접 검사에는 두 번째 이미지와 비동기 파일/프로세스 경로가 빠져 있었다. 첫 실행 지연이라는 이전 추정은 확정 원인이 아니다.
- 운영 코드는 변경하지 않고 첫 정상 읽기 테스트에 제한된 이벤트 관찰을 추가했다. 이미지 번호와 호출 순서, 파일 읽기, 디코더 입력·출력·종료 상태를 실패 시 기록한다. 기존 4.5초 제한·기대값·실패 판정은 유지하며, 테스트 후 원래 프로세스 실행 함수를 복원한다.
- `images` 선택지를 추가해 한 명령으로 관련 파일만 실행할 수 있다. 기본 전체 검사 범위는 그대로이며 임의 파일/Node 옵션이나 실제 DB 검사는 허용하지 않는다.
- 로컬에서 지연 프로세스를 주입해 원래 실패가 그대로 유지되고 `IMAGE_LOAD_TRACE`가 파일 읽기 완료·디코더 시작·입력 완료·출력 없음까지 구분하는 것을 확인했다. 실제 바이너리를 교체하거나 운영에 모의 프로세스를 연결한 것이 아니다. Replit의 해당 추적 결과를 받아야 실제 정체 위치를 확정할 수 있다.
- 전체 로컬 재검증 중 정산 종료 검사 3개가 별도로 실패했다. `cancel`은 실제 현재 날짜를 기록하는데 후속 종료 검사는 고정 `2026-09-16T12:00:00Z`를 사용해, 실제 시각이 이를 지나면 정상적인 역행 방어에 걸렸다. 해당 테스트 파일의 Date만 시나리오 시작 시각에 고정했다. 실제 타이머·정산 로직·역행 거부 조건은 그대로다. 이미지 지연의 원인이나 해결로 취급하지 않는다.
- 보완 후 Windows / Node 24.15.0의 기존 로컬 의존성에서 기본 **538개(안전 51 + 서버 483 + 클라이언트 4), 실패·생략 0**, 이미지 단독 20개 및 서버 테스트 타입/실행기 구문/공백 검사를 통과했다. 운영 소스·SQL·잠금 파일·Replit 설정에는 차이가 없다. 전체 제품 빌드·Replit 실환경 성공으로 확대 해석하지 않는다.

## 6단계 Replit 빌드 통과·관찰 없는 이미지 검사 분리

- 사용자가 진단본에서 이미지 단독 20개, 이후 서버 483개와 클라이언트 4개의 실패·생략 0 결과를 제공했다. 이어 전체 타입 검사와 API·ShowMe·mockup 빌드 성공 출력을 확인했다. ShowMe의 기존 세 UI 파일 소스맵 위치 경고는 남아 있다. 이 기록은 사용자가 실행한 Replit 결과이며 이 PC의 실행 결과와 구분한다.
- 관찰 추가 후 통과했다는 사실만으로 지연 원인을 해결했다고 판정하지 않는다. 관찰에 따른 실행 타이밍 변화 가능성을 분리하기 위해 원래 정상 읽기 검사 본문을 첫 검사 `(without tracing)`로 복구하고 관찰 포함 검사는 두 번째 독립 검사로 유지했다. 첫 본문이 `86473c7`의 원래 본문과 같음을 TypeScript 구문 트리에서 추출해 비교했다. 저장소/조회 래퍼나 프로세스 관찰은 첫 검사에 설치되지 않는다.
- 두 검사 모두 실제 로컬 이미지 읽기·FFmpeg 해독·바이트 일치·원본 상태 보존·반환 버퍼 독립성을 확인한다. 어느 쪽이든 실패하면 전체가 실패한다. 기대값·기본 4.5초 제한·운영 소스·기존 테스트 범위는 바꾸지 않았다. 이미지 그룹은 20→21개, 기본 서버 검사는 483→484개다.
- 시험 자식 프로세스 안에서만 해독 동작을 세 가지 오류로 교체했다: 종료 코드 1, 종료 코드 0이지만 출력 1바이트, 15초 정체. 관찰 없는 정상 읽기 검사는 세 조건 모두 `ANALYSIS_IMAGE_UNAVAILABLE`와 종료 코드 1로 실패했고 진단 기록은 없었다. 이는 실패를 숨기지 않는 검사인지 확인하는 고의 실패 시험이며 실제 바이너리·운영 파일을 변경한 것이 아니다. 정체 시험도 원래 4.5초 제한을 사용했다.
- Windows / Node 24.15.0의 기존 로컬 의존성에서 이미지 **21개**, 기본 전체 **539개(안전 51 + 서버 484 + 클라이언트 4), 실패·생략 0** 및 서버 테스트 타입 검사를 통과했다. 전체 로그는 Git 제외 `outputs/migration-verification/tests-no-trace-windows.log`에 있다. 제품 빌드는 이번 테스트-only 변경 후 이 PC에서 재실행하지 않았다.
- 다음은 Replit에서 이 보완본의 `images` 검사를 받아 첫 `(without tracing)`까지 통과하는지 확인하는 것이다. Replit에서 이 새 검사를 아직 실행하지 않았으며 과거 시간 초과의 근본 원인은 미확정이다. 실제 DB/Storage/Gemini 호출·main 병합·배포는 하지 않았다.

## 8단계 Replit 실패 재현·Linux 대기 상태 진단 — 2026-09-17

- 사용자가 `a2b7581` 진단 결과를 제공했다. 21개 중 19개 통과/2개 실패/생략 0이며 첫 무관찰과 두 번째 관찰 포함 정상 읽기가 모두 약 4.5초에 실패했다. 3.5초 기록은 입력 22,711바이트 쓰기 완료/출력 0/프로세스 미종료였다. 파일 읽기·프로세스 시작은 수 ms였고, 같은 자료의 뒤쪽 디코딩은 성공했다. 부모 쪽 쓰기 완료가 FFmpeg의 실제 입력 읽기 완료를 의미하지는 않는다.
- 사용자의 `command -v`, `readlink -f`, `file -L` 결과로 호스트 FFmpeg 8.1.2가 Nix 경로의 동적 링크된 x86-64 ELF임을 확인했다. 셸 래퍼 지연은 지지되지 않지만 동적 로더/파일 I/O/입력 대기/스케줄링 중 원인은 미확정이다.
- 기존 3.5초 진단 지점에서 해당 실행의 살아 있는 해독 자식만 비동기로 `/proc` 상태를 읽도록 추가했다. 부모·PID·시작 시각 확인, 종료 시 표본 폐기, 필드 허용 목록, 읽기 크기 제한과 오류 비노출을 적용했다. stdout 소비·spawn 교체·사전 워밍업·4.5초 제한 변경은 없다. 상태 수집의 타이밍 영향은 여전히 가능하며 원래 이미지 검사와 운영 소스는 바꾸지 않았다.
- Windows Node 24.15.0의 기존 의존성에서 새 진단 보호 검사 7개를 포함한 안전 검사 **58개**, 원래 이미지 **21개**, 별도 진단 이미지 **21개** 및 구문/공백 검사를 통과했다. 별도 정체 자식 시험에서 spawn 동일성·stdout 리스너 무추가·가상 비밀 인수/입력 비출력을 확인했고 3.5초 관찰 뒤 4.5초에 종료한 자식의 실패 종료 코드 1을 유지했다. Windows는 `not-linux`로 보고하며 실제 Linux `/proc` 읽기는 이 PC에서 검증하지 않았다. 전체 546개/제품 빌드는 이번 재실행 결과가 아니다.
- 다음은 Replit에서 OS 수집 보호 검사를 먼저 실행하고, 같은 이미지 진단을 한 번 실행해 `IMAGE_PROCESS_OS_STATE`와 실패 내역을 함께 확인하는 것이다. 비재현은 원인 해결로 간주하지 않는다. 실제 DB/Storage/Gemini 호출·환경 변경·main 병합·배포는 없다.

## 7단계 간헐적 실패 유지·프로세스 상태 진단 — 2026-09-17

- 사용자가 Replit의 새 첫 무관찰 검사에서 약 4567ms 뒤 실패한 결과를 제공했다. 이후 별도 프로세스의 관찰 포함→없음→없음→포함 비교는 모두 통과했다. 환경은 Node **24.13.0 / Linux**이며 각각 검사 시간은 1149/547/595/569ms였다. 관찰 없이는 반드시 실패한다는 가설은 지지되지 않지만, 첫 실행/환경 부하/스트림 중 어느 것이 원인인지도 확정되지 않았다. 성공 결과로 앞선 실패 기록을 지우지 않는다.
- 기본 테스트·운영 소스·시간 제한을 유지하고 별도 `scripts/diagnose-images.mjs`를 추가했다. 동일한 이미지 파일을 실행하면서 Node 진단 채널로 생성된 프로세스 참조를 얻는다. 기존 관찰과 달리 `spawn` 교체, 저장소/조회 함수 감싸기, stdout 데이터 리스너 추가를 하지 않는다. 3.5초 뒤 아직 살아 있는 해독 프로세스와 종료 시점의 입출력 카운터/상태만 읽는다. 비밀값·경로·인수·입출력 내용은 기록하지 않는다. 이 진단에도 관찰 영향 가능성은 남으며 기본 검사의 대체가 아니다.
- 로컬 검증에서 `spawn` 함수 동일성 및 추가 stdout 데이터 리스너 0개, 실제 수신 바이트 수, 가상 비밀 문자열 비출력을 확인했다. 없는 FFmpeg 경로를 자식 프로세스에만 제공했을 때 진단 실행기도 종료 코드 1을 유지했다. 지연 해독 대역에서는 약 3512ms에 입력 22,711바이트 전달 완료/출력 0 상태를 기록하고 기존 4.5초 제한에서 원래 검사가 실패했다. 대역은 검증 자식 프로세스에만 주입했고 실제 바이너리는 바꾸지 않았다.
- Windows / Node 24.15.0의 기존 의존성으로 별도 진단의 이미지 21개 통과를 확인했다. 이 실행은 Replit의 Node 24.13.0/호스트 FFmpeg 8.1.2 재검증이 아니며, 버전 차이를 원인으로 단정하지 않는다. 다음은 Replit에서 새 진단을 한 번 실행해 실패 내용과 `IMAGE_PROCESS_WAIT`/`IMAGE_PROCESS_FINAL`을 함께 확인하는 것이다. 진단이 통과하면 비재현으로 기록하며 원인 해결을 선언하지 않는다.
