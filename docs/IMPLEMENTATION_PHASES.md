# ShowMe 구현 게이트

각 단계는 아래 완료 조건과 자동 검증을 통과한 뒤에만 다음 단계로 넘어갑니다. UI 데모가 보인다는 이유만으로 뒤 단계를 완료 처리하지 않습니다.

이 문서의 완료 표시는 **코드와 로컬 검증**에 한정합니다. PostgreSQL/Replit App Storage 실서비스 검증, 실제 브라우저 화면 공유 권한, 운영 배포는 Gate 7에서 별도로 확인합니다. 현재 공개 사이트는 이전 프로토타입입니다. 2026-09-12 독립 리뷰 결과는 [리뷰 기록](REVIEW_2026-09-12.md)을 참고하세요.

전체 순서와 현재 착수 지점은 [전체 개발 계획 v1.7](DEVELOPMENT_PLAN.md), 데이터 계약·실패 처리·완료 기준은 [기능별 상세 설계](NEXT_PHASE_DESIGN.md), AI 큐·비용 예약은 [실행 설계](ANALYSIS_EXECUTION_DESIGN.md)가 기준입니다. Gate 2 실기기 확인은 전체 계획 A0로 남기고, Gate 3–7과 후속 모바일 제작 화면을 각각 구현·검증·리뷰 단위로 진행합니다. 설계 문서의 제안은 구현 완료를 뜻하지 않습니다.

2026-09-14 사용자 정책 확정: AI·서버는 현재 무료만, 가상 개인정보가 들어간 사용자 제공 예정 자료로 시험, 재치 있는 모자이크, 공유 가이드 15일, 원본은 제작자 opt-in, 지인 테스트부터 시작합니다. 서비스 시작 시 유료 전환 의사는 기록하되 금액·계정/플랜 변경은 별도 확인합니다. 이 갱신은 **설계 문서만 변경**했으며 자료 전송·가림/만료 기능 구현·과금 변경·배포를 하지 않았습니다. 기존 185개 테스트 기록은 이전 B1 검증이며 이번 문서 갱신에서 전체 테스트를 재실행하지 않았습니다.

## Gate 0 — 실행 구조와 데이터 경계 ✅

- Sites UI와 장시간 미디어 프로세서를 분리한다.
- Replit 배포에서는 PostgreSQL과 App Storage가 없으면 시작하지 않는다.
- 원본 영상 object key와 편집 키를 공개 응답에 노출하지 않는다.
- DB migration과 Replit build/run 명령을 저장소에 고정한다.

검증: 중앙 설정 테스트, 시작 readiness, DB·Storage startup probe, 운영 FFmpeg 버전 검사.

## Gate 1 — 안전한 영상 수신과 작업 복구 ✅

- MP4/MOV/WebM을 스트리밍으로 받고 500 MiB에서 차단한다.
- 지원 브라우저에서는 창·탭·전체 화면을 직접 녹화해 같은 WebM/MP4 업로드 흐름으로 넘긴다. 직접 녹화는 브라우저 메모리 안전을 위해 4분/64 MiB로 제한하고, 긴 영상은 파일 업로드를 사용한다.
- 브라우저가 업로드 전에 guide ID와 edit token을 보관한다.
- 복구 키는 가이드별로 분리하고 현재 탭의 작업을 우선 복구한다. 직접 녹화에서는 오디오를 수집하지 않는다.
- 같은 업로드를 다시 보내도 새 가이드나 고아 원본을 만들지 않는다.
- bounded queue, 재시도 coalescing, 최대 처리 시도 횟수를 적용한다.
- 프로세스 재시작과 겹친 worker에서 stale attempt가 승자의 상태나 자산을 변경하지 못한다.
- 취소가 업로드보다 먼저 도착하면 24시간 tombstone을 남긴다. 원본과 프레임은 수동 전체 삭제를 지원하고, 미공개 ready/failed 초안은 마지막 상태 갱신으로부터 7일 뒤 자동 정리 대상이 된다.
- 저장이 시간 초과 후 늦게 끝나도 삭제 기록을 유지한다. 삭제 실패 기록은 다음 정리 주기로 넘기고, 갱신된 생존 신호와 다른 처리 작업의 삭제 권한을 존중한다.

검증: API, multipart cleanup, retry race, queue capacity, CAS/ABA, recovery 테스트.

## Gate 2 — 실제 FFmpeg 장면·프레임 처리 ✅

- ffprobe로 길이, 코덱, FPS, 회전 메타데이터를 검증한다.
- scene detection과 interval fallback으로 최대 24개 단계를 만든다.
- iPhone 회전 메타데이터를 정확히 한 번 적용한다.
- 세로·가로 비율의 대표 프레임과 thumbnail을 영속 저장한다.
- 길이·해상도·픽셀·FPS·코덱 제한으로 비정상 입력을 조기에 중단한다.
- 업로드 영상 입력마다 file 프로토콜 및 MP4/MOV/WebM 컨테이너 allowlist를 적용한다. 위장된 재생목록이 외부 URL을 여는 것을 차단한다.
- 각 처리 시도의 최대 100개 단계 키를 정리해 설정 변경 후에도 과거 자산을 추적한다. 다운로드·삭제 대기는 제한 시간 안에 끝내고, 늦게 완료된 로컬 복사도 제거한다.
- 현재 단계의 설명과 중앙 포인터는 임시 표시다. UI에서 AI 설명·개인정보 탐지·가림·편집 저장이 아직 적용되지 않았음을 명시하고 실제 영상의 공개를 차단한다.

검증: 실제 FFmpeg fixture, 회전 영상, 손상 영상, resource limit, superseded attempt 테스트 및 수동 API E2E.

## Gate 3 — AI 초안 생성 ⏭️

- provider interface 뒤에서 Gemini 이미지 입력·구조화 출력을 호출한다(2026-09-12 사용자 선택).
- 각 단계의 한국어 설명, 클릭 위치, 개인정보 후보를 percent 좌표로 반환한다.
- schema validation, timeout, retry, 비용·프레임 상한을 적용한다.
- AI 실패가 미디어 처리 결과나 원본을 손상시키지 않는다.

진행 상황: **3A 계약·저장 기반, 3B-1 Gemini 가상 화면 실제 연결 확인, 3B-2A HTTP API, 전체 계획 B1–B4-B3 저장·내부 자동 처리·종료 예산·날짜 경계 로컬 완료**. 입력/출력 검증, 제한된 재시도·타임아웃, 별도 로컬 시험용 하루 10회 예약 한도를 유지한다. 기본 시험은 외부 통신을 하지 않으며 실제 호출은 키와 무료 등급 확인·가상 화면 전송 동의가 필요하다. `gemini-3.8-flash` 기본값은 그대로이고 승인된 `gemini-3.5-flash-lite`를 명시적으로 시험할 수 있다. 2026-09-14 앞선 단계에서 3.5로 가상 화면 2장의 한국어 설명·좌표·결과 저장을 확인했다. 서버는 3.5 모델과 기본 접수기를 연결했지만 **실제 준비 조건 검증기가 없어 새 요청은 계속 503이며 Google을 호출하지 않는다.** 제품 내 동의·운영 실행 연결·실제 PostgreSQL/무료 조건 검증은 남아 있어 Gate 3 전체 완료는 아니다. [3A 기록](GATE_3A_CHECKPOINT.md), [Gemini 설정](GEMINI_SETUP.md), [API 계약](ANALYSIS_API.md)을 참고한다.

2026-09-14 비교 기록: 3.8 모델 정보 조회는 HTTP 200이지만 분석 2회는 HTTP 503, 이후 승인받은 3.5 Flash-Lite 분석 1회는 성공했다. 해당 시험까지 총 예약은 3/10회다. 두 모델의 한도를 공유하며 자동 전환은 하지 않는다. 이번 API 단계에서는 추가 Google 호출이 없었다.

3B-2A 검증: 요청·조회·취소, 소유자 인증·동의 형식·동시 중복·원자적 최초 초안/실행 저장·취소 후 늦은 결과 차단·삭제 경합·오류 비노출을 검사했다. 새 API 테스트 17개를 포함해 processor 171개 + client 4개, **총 175개**, 타입 검사·빌드·lint 통과. 실행 접수기는 시험용이며 실제 PostgreSQL/운영 큐·비용 예약 검증은 아니다. 다음은 내구성 있는 AI 큐·운영 예산 접수기 연결 단계다.

2026-09-14 전체 설계 v1/B1: 전체 개발 계획을 웹 완성·모바일 제작 보완까지 정리했고, 다음 실행 단계의 원자 접수·예산·묶음 복구 계약을 구체화했다. 첫 구현으로 예산 최대분 계산·여유 검사·불확실 사용량 보존 순수 함수와 테스트 10개를 추가했다. **총 185개 테스트**(processor 181 + client 4), 타입·lint·빌드 통과. 운영 한도 값·DB 예약·큐·실호출 연결은 여전히 미구현이며 새 분석은 503을 유지한다. 다음 작업은 전체 계획 B2다. [설계와 B1 검증](ANALYSIS_EXECUTION_DESIGN.md).

2026-09-14 B2-A: 초안·분석 실행·최대 6묶음·전체/가이드 일일 예산 예약을 함께 저장하는 내부 계약과 migration `0003`을 추가했다. 중복 20건, 한도 경합, 저장 실패·응답 유실·재열기, 날짜 변경, 삭제 후 재생성에서도 중복 예약/한도 초기화가 없는지 단일 JSON 저장소로 확인했다. PostgreSQL은 구현·스키마 및 삭제 호출 순서 모의 검증까지만 했으며 실제 DB 연결·migration·경합/롤백은 B5에 남긴다. **총 202개 테스트**(processor 198 + client 4), 타입·lint·빌드 통과. JSON v2는 기존 v1을 다음 쓰기에 승격하며, 예전 프로그램으로 버전만 낮춰 복원하면 안 된다. API·Google·과금·공개 Site는 활성화하지 않았다. 다음은 **B2-B 개별 요청 원장·사용량 정산**이고 Gate 3 전체는 여전히 진행 중이다. [상세 체크포인트](ANALYSIS_EXECUTION_DESIGN.md).

2026-09-14 B2-B: 요청별 예약 배정·전송 기록·확정/불확실 사용량 정산·중복 차감 방지·종료 후 미전송 요청 해제를 구현했다. 상한 초과는 최대분과 전체 중단 상태를 함께 보존하며 재시작·날짜 변경·삭제로 해제하지 않는다. JSON은 기존 v1/v2 자료를 보존하는 v3로 확장했고 PostgreSQL migration `0004`를 추가했다. 신규 정산 테스트 25개를 포함해 **총 227개**(processor 223 + client 4), 타입·lint·빌드 통과. PostgreSQL 검증은 호출 순서/트랜잭션 경계 모의 시험이며 실제 DB 검증은 아니다. 미배정 예약의 종료 정리·날짜를 넘는 전송·lease 결합은 B4, 실제 DB와 제한된 전송 검증은 B5에 남는다. 다음은 **B3 내구성 있는 접수기**다. API/시작 코드·실제 Google 호출·과금·푸시·공개 Site 배포는 변경하지 않았다. [B2-B 상세 계약](ANALYSIS_EXECUTION_DESIGN.md).

2026-09-14 B3: 기본 분석 접수기를 API에 연결해 최초 초안·실행·묶음·예산을 함께 저장하도록 구성했다. 저장 직전 준비 조건의 만료·철회·날짜 변경·중단을 다시 확인하고, 중복 요청은 기존 예약을 재사용한다. 기본은 무료 전용이며 실제 준비 조건 검증기가 없어 새 요청은 계속 503이다. 신규 접수 테스트 24개를 포함해 **총 251개**(processor 247 + client 4), 타입·lint·빌드 통과. 성공 경로는 가상 준비 보고서와 로컬 JSON 저장소로 검증했고 PostgreSQL은 트랜잭션 경계 모의 시험만 했다. 실제 DB·Google 호출·과금·UI·푸시·배포는 변경하지 않았다. 다음은 **B4 작업 실행·재시작 복구**다. [B3 상세 계약](ANALYSIS_EXECUTION_DESIGN.md).

2026-09-14 B4-A: B4를 소유권 저장과 실제 처리 연결로 나누고, 이번에는 제한된 대기 작업 조회·전체 funded 소유권 1개·최대 3회 인계·만료된 요청의 불확실 비용 보존을 구현했다. 인수 이력이 있는 작업의 배정/전송 기록에는 정확한 유효 소유권을 요구하며, 예산 없는 예전 실행기를 통한 우회는 계속 막는다. PostgreSQL migration `0005`는 기존 payload를 조회 열로 채운 뒤 일치 제약·인덱스를 적용하도록 생성했고, 실제 DB에 적용하지 않았다. JSON v3 유지. 신규 24개를 포함해 **총 275개 테스트**(processor 271 + client 4), 타입·lint·빌드 통과. PostgreSQL은 모의 쿼리/트랜잭션 검증이며 실제 SQL 엔진 검증이 아니다. 자동 실행 루프·AI 호출·부분 결과 저장·종료 예약 정리·날짜를 넘는 예산 확보는 다음 **B4-B**다. 기본 신규 분석 503·무료 전용 정책을 유지했고 환경변수·과금·UI·푸시·배포는 변경하지 않았다. [상세 체크포인트](ANALYSIS_EXECUTION_DESIGN.md).

2026-09-14 B4-B1: 묶음별 결과와 확정 사용량을 함께 저장하고, 모든 묶음이 완료될 때만 전체 결과와 최초 미수정 초안을 확정하도록 구현했다. 중단·인계 후 완료 묶음은 보존하며 중복 결과의 재적용/중복 정산을 차단한다. 사용자가 수정한 초안은 덮어쓰지 않는다. 잘못된 출력은 안전하게 실패 처리하고 상한 초과는 최대 예약·전체 중단과 함께 보존한다. JSON v4는 기존 v1/v2/v3 자료를 보존하며 PostgreSQL migration `0006`은 상태 backfill·일치 제약을 추가한다. 실제 DB에 적용하지 않았다. 신규 28개를 포함해 **총 303개 테스트**(processor 299 + client 4), 타입·lint·빌드 통과. PostgreSQL은 모의 트랜잭션 검증이며 실제 SQL 엔진/다중 연결 시험이 아니다. 기본 신규 분석 503·무료 전용 정책을 유지했고 AI 호출·환경변수/과금·UI·푸시/배포는 변경하지 않았다. 다음은 **B4-B2 자동 실행·복구 연결**이다. [최신 체크포인트](ANALYSIS_EXECUTION_DESIGN.md).

2026-09-15 B4-B2: 명시적 내부 자동 루프·중복 실행 합치기·완료 묶음 건너뛰기·미전송 예약 재사용·제한된 5xx 재시도·저장 오류 backoff를 구현했다. 이미지 로딩/실제 전송 직전 소유권·입력·취소·무료 준비 조건을 다시 확인하고 처리 중 취소/삭제/시간 초과를 감시한다. 실패와 남은 sending의 불확실 사용량을 함께 저장하며, 애매한 복구 전송은 무조건 다시 보내지 않는다. 전체 336개 테스트(processor 332 + client 4, 신규 33개)·타입·lint·빌드 통과. Gemini 어댑터는 실제 fetch를 시험 응답으로 교체했고 PostgreSQL도 트랜잭션 대역만 사용했다. JSON v4/PostgreSQL `0006` 유지, 실제 DB/Google·환경변수/과금·UI·푸시/배포 변경 없음. 실환경 검증기·비공개 이미지 로더·서버 자동 시작은 연결하지 않아 새 분석은 계속 503이다. 다음은 **B4-B3 종료 예산·날짜 경계**이며 Gate 3 전체 완료가 아니다. [최신 체크포인트](ANALYSIS_EXECUTION_DESIGN.md).

2026-09-15 수정→검증→B4-B3→검증: 최종 상태 읽기 중 취소가 저장돼도 전송되던 경합을 수정했다. 실제 전송 시작을 취소/삭제와 같은 잠금 안에 넣고 응답 대기는 잠금 밖에서 수행한다. `02cf3aa`에서 먼저 전체 340개·타입·lint·빌드를 통과했다. 이어 종료 후 미배정/reserved 예산 반환, 불확실 전송 최대분 보존, 전날 queued/만료 running 종료와 새 요청의 새 날짜 예산, 저장된 5xx 근거에 따른 한 번의 재시도, 부적격 선두 뒤 후보를 찾는 순환 조회를 구현했다. JSON v5/PostgreSQL `0007` 호환 코드를 추가했고 실제 DB에 적용하지 않았다. 새 20개 포함 **전체 360개(processor 356 + client 4)**·타입·lint·빌드 통과. SQL 엔진/다중 연결·실제 AI·무료 조건 검증은 아직 아니며 환경변수/과금·UI·푸시/배포도 변경하지 않았다. 새 분석은 503 유지, 다음은 **B5 실제 DB·제한된 연결 검증**이다. [최신 상세 기록](ANALYSIS_EXECUTION_DESIGN.md#15-전송-경합-수정--검증--b4-b3--검증--2026-09-15).

## Gate 4 — 서버 저장 편집기


- 제목·단계·문구·클릭 위치·가림 영역 변경을 edit token 권한으로 저장한다.
- 직접 조작 StepCanvas와 키보드 접근성을 제공한다.
- 새로고침·다른 화면 크기에서도 percent 좌표가 유지된다.

## Gate 5 — 개인정보 비가역 처리와 게시

- 게시 전 모든 가림 후보를 확인하게 한다.
- 사용자 선택에 따라 원본 픽셀과 무관한 불투명 모자이크를 파생 이미지에 구워 넣는다. 실제 가림 구현·검증은 아직 남아 있다.
- 게시 snapshot과 편집 draft를 분리한다.
- 원본 공유는 기본 꺼짐이며 별도 권한 경로로만 제공한다.

## Gate 6 — 공개 링크와 모바일 뷰어

- 공개 slug API와 `/g/:slug`가 동일한 게시 snapshot을 표시한다.
- 공개 응답에는 edit token, 원본 object key, 미가림 frame이 없다.
- 카카오톡 공유용 metadata와 모바일/데스크톱 뷰어를 검증한다.

## Gate 7 — 실제 배포와 회귀 검증

- Replit Reserved VM에 processor를 배포하고 production Secrets를 등록한다.
- Sites 빌드에 HTTPS processor URL을 주입한 뒤 한 번만 다시 게시한다.
- MP4/MOV/WebM, 세로/가로/회전, 재시작 복구, 500 MiB 경계를 순서대로 확인한다.
- 실제 processor가 준비되기 전에는 현재 공개 Sites를 새 frontend로 덮어쓰지 않는다.
